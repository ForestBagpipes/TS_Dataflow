# -*- coding: utf-8 -*-
"""Remaining paragraphs: contributions, 2.4 module descriptions, 6.1 stage zero
findings, and the plain text formulas still sitting inside the algorithm boxes."""
import sys
sys.path.insert(0, "tools")
import docx
from docx_rewrite import count_math, find_paragraph, set_paragraph_md

PATH = "docx/胡宏彬-进度文档-20260819.docx"


def main():
    d = docx.Document(PATH)
    before = count_math(d)

    def rw(prefix, md):
        set_paragraph_md(d.paragraphs[find_paragraph(d, prefix)], md)

    # 贡献
    rw("（1）把时序数据治理形式化为屏蔽约束下的序贯干预决策",
       "（1）把时序数据治理形式化为屏蔽约束下的序贯干预决策，并给出对应的智能体 IntroAct-TS。"
       "该形式化把判断从动作执行之前推迟到执行之后，"
       "因而不要求事先写出只关于动作本身的安全规则，"
       "同时使策略能够从执行后的反馈中改进。")
    rw("（2）提出了以屏蔽作为奖励塑形器的策略学习机制",
       "（2）提出以屏蔽作为奖励塑形器的策略学习机制。"
       "屏蔽在拦下破坏性动作的同时决定了哪些奖励可以到达策略，"
       "使策略学到的是能通过结构检验的修复方式，"
       "避免了直接以模型效用为奖励时策略转向抹平数据的失效模式。"
       "本文用奖励塑形消融实验对这一机制作直接检验。")
    rw("（3）证明了探索过程的结构安全性",
       "（3）证明了探索过程的结构安全性。"
       "由于候选动作在副本上执行且提交需经屏蔽，"
       "对任意策略以及任意探索强度，被提交编辑的结构失真都受同一阈值约束。"
       "这使智能体可以在真实语料上试错而不破坏语料，"
       "与算子立即生效的强化学习清洗方案形成对照。")

    # 2.4 三个模块描述里的第一人称与冗余
    rw("为应对缺少质量标签且单一证据存在盲区的困难",
       "缺少质量标签且单一证据存在盲区，基于同类校准的双证据状态构建算法（CDP）因而"
       "从窗口自身提取统计画像，同时用冻结基础模型的前向探测获取行为签名，"
       "再以画像相近的邻居为参照对行为分量做稳健标准化，"
       "使比较发生在结构相似的窗口之间。"
       "CDP 产出的状态包含统计分量、行为分量与缺陷类型后验，"
       "既是策略的输入，也为动作选择划定搜索范围。")
    rw("在状态给定之后，为回答某个具体动作施加下去会发生什么",
       "状态给定之后，回答某个具体动作施加下去会发生什么，"
       "同时不让这次尝试造成不可撤销的后果，由基于沙箱副本的干预重测与奖励生成算法（SIR）承担。"
       "它把候选动作作用在工作副本的拷贝上而不覆盖原值，"
       "随后用与事前完全相同的探测函数测量候选结果。"
       "SIR 的关键约束是两次测量必须落在同一留出跨度与同一归一化尺度上，"
       "满足这一条件效用差值才能既作为裁决依据又作为奖励读数。")

    # 6.1 阶段零三条，去掉实测数字，留结论
    rw("固定规则下的重试净效果为负",
       "固定规则下的重试净效果为负。更换动作的成功率不足首次提议的十分之一，"
       "原因是备选顺序与窗口特性无关。"
       "这一结果构成了引入策略学习的直接依据，"
       "序贯决策的价值需要策略能够依据撤销原因调整方向才能兑现。")
    rw("可学结构集中在单一算子上",
       "可学结构集中在单一算子上。按画像簇与算子交叉统计固定规则策略的接受率，"
       "插补的接受率在簇之间存在可检出的差异，而重分段在任何簇数下都没有，"
       "去尖峰随簇数增大迅速失去显著性，更像分辨率不足而非结构，"
       "去噪的候选数太少且低期望频数格子过多，不作为独立证据。"
       "策略学习的收益因而主要来自对插补提议的取舍，而非全部算子。")
    rw("固定规则策略存在系统性盲区",
       "固定规则策略存在系统性盲区。在簇与算子的全部组合中有四分之一从未被访问，"
       "其中两格合计覆盖语料近两成的窗口，而固定规则在这些窗口上一次也没有提议过对应算子。"
       "这是确定性策略的结构性缺失而非数据稀疏，离线无法对这些格子估值，"
       "只能依靠在线探索填补，这也是引入乐观初始化的直接理由。")
    rw("画像簇不是分层标签的重述",
       "画像簇不是分层标签的重述。簇与分层标签的调整互信息很低，"
       "说明画像几何捕捉的结构与人工分层基本正交。"
       "若两者高度一致，则接受率随簇变化这一现象将只是接受率随分层变化的换一种说法，"
       "而分层信息 CDP 已经以缺陷假设的形式给出，策略学习不会带来新信息。")

    # 6.3 进度
    rw("前三个模块的实现已经完成并通过测试",
       "前三个模块的实现已经完成并通过测试，状态构建、沙箱执行与双信号屏蔽均可运行，"
       "治理轨迹可完整导出。共形标定模块已实现，损害记录已缓存落盘，"
       "调整目标风险水平不需要重新占用加速器。"
       "两个已发表的清洗方法已作为提议者接入，三种裁决方式对照均已完成。"
       "策略优化与预算分配模块的价值表与暖启动已经实现，"
       "所需的画像簇划分复用状态构建阶段的近邻检索结果，"
       "奖励信号直接从已有的治理轨迹中提取，这一模块不需要重新采集数据。"
       "待完成的工作包括上置信界选择与预算分配接入治理循环、"
       "打分式基线的接入与统一语料层协议的跑通。")

    d.save(PATH)
    d2 = docx.Document(PATH)
    print(f"尾部段落完成，公式 {before} -> {count_math(d2)}")


if __name__ == "__main__":
    main()
