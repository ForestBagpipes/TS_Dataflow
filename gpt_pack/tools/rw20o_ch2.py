# -*- coding: utf-8 -*-
"""Sections 2.1, 2.3 and 2.4. These carry equations, so every paragraph goes
back through the markdown path rather than through a text substitution."""
import sys
sys.path.insert(0, "tools")
import docx
from docx_rewrite import count_math, find_paragraph, set_paragraph_md

PATH = "docx/胡宏彬-进度文档-20260820.docx"


def main():
    d = docx.Document(PATH)
    before = count_math(d)

    def rw(prefix, md):
        set_paragraph_md(d.paragraphs[find_paragraph(d, prefix)], md)

    rw("衡量模型是否变得更容易处理一段数据",
       "要知道模型是不是更容易处理这段数据了，得有办法从模型身上读出证据。"
       "行为探测就是干这个的，它不更新任何参数，只看冻结模型对一个窗口作何反应。"
       r"记窗口为 $x$，冻结模型为 $f$，探测函数 $\mathrm{Probe}(f,x)$ 输出一个固定维度的行为签名 $b$，"
       "里面装着逐位置预测误差的各个分位数、层间表示的余弦距离、遮挡之后能不能恢复、"
       "多个视图的预测一致到什么程度、轻微扰动前后输出变了多少，"
       "以及模型池里几个模型彼此有多大分歧。"
       "签名之所以不能只放预测误差，是因为被噪声污染的窗口和本来就难预测的窗口都会给出高误差。"
       "前者的误差堆在少数几个位置上，后者的误差摊得很匀，分位数信息刚好能把这两种分开。"
       "探测只走前向，不算梯度，单次开销远低于要求海森求逆的影响函数方法[11]，"
       "这一点在需要反复测量的强化学习设定下直接决定了方法跑不跑得动。")

    rw("有了行为签名，模型侧的度量可以定义出来",
       "有了签名，模型这一侧的尺子就能定下来。"
       "模型效用衡量一个窗口对目标模型有多友好，取若干归一化误差分量的加权负和")

    rw("其中  取自行为签名",
       r"其中 $g_{k}$ 取自行为签名 $b$ 的第 $k$ 个误差分量，$w_{k}$ 是它的权重。"
       r"各分量都是越小越好，整体取个负号之后，$U$ 越大就表示这个窗口对模型越友好。"
       r"一次候选动作 $a$ 带来的效用变化写作")

    rw("该差值只有在两次测量使用相同的留出跨度",
       "这个差值要有意义，前提是两次测量落在同一段留出区间上，用的还是同一个归一化尺度。"
       "改变窗口长度的动作会破坏这个前提，所以同跨度同尺度是进入判据的硬要求，"
       r"具体怎么做见 3.2 节。$\Delta U$ 既是本文奖励信号的来源，"
       "也是 2.2 节说的那个偏差的载体，"
       "一边用它一边不被它带偏，是整套方法设计的核心。")

    rw("效用只回答了模型是否更容易处理这段数据",
       "效用只回答了模型是否更省力，而把序列削平同样能让模型更省力，"
       "所以还得有一把不看模型的尺子。"
       "时序结构保真度衡量一次编辑把序列固有的结构破坏了多少，数值越低说明保得越好，"
       "它只比对编辑前后的时序属性。"
       "比对哪些属性取决于算子动了多大范围。"
       "只改有限几个位置的算子，在它声明的足迹之外比形状、极值、补片衔接得自不自然、"
       "足迹本身多大以及局部离散程度，"
       "把整条序列重写一遍的算子则比趋势、主周期、频谱能量分布、自相关结构、变点位置和极值。"
       r"记编辑前后的窗口为 $x$ 与 $a(x)$，第 $j$ 个属性上的归一化差异为 $d_{j}$，"
       r"对应权重为 $w_{j}$，结构失真定义为")

    rw("式中  是第  个属性在编辑前后的取值",
       r"式中 $d_{j}$ 是第 $j$ 个属性在编辑前后的取值按该属性自身量纲归一化之后的绝对差，"
       r"落在 $[0,1]$ 之间，权重 $w_{j}$ 按属性对该类算子的相关程度给定。"
       "加权平均之外还留了最差的那一项，是为了防止某一项上的灾难被其余项平均掉。"
       "序列尾部被整段抹平就是这种情况，多数属性上看着变化不大，只有极值那一项会露馅。"
       r"$D$ 与模型无关，因而能独立于效用信号提供约束，"
       "策略在学习过程中也没法操纵它。")

    rw("两个度量都有了，还需要确定它们在哪里被施加",
       "两把尺子都有了，还得定下它们在哪一步生效。"
       "屏蔽[14]的做法是在智能体和环境之间加一层过滤，"
       "动作生效之前先查它有没有违反给定的规约，通过了才放行。"
       "屏蔽有两个基本要求，一是可靠，违规的动作不会漏过去，"
       "二是不阻塞，任何状态下至少还留着一个可以放行的动作。"
       "屏蔽和策略学习是分开的，底下用什么算法都行，"
       "安全性每一步都成立，不用等训练收敛。")

    rw("屏蔽给出了施加判断的位置",
       "位置定了，阈值还得有个来处，而在无标签场景里这个数没法靠领域知识拍出来。"
       "共形风险控制[21]不对数据分布作任何假定，"
       "就能为由阈值参数化的一族决策给出有限样本的风险保证。"
       r"给定一个与部署数据可交换的校准集、一个有界的风险函数和目标风险水平 $\alpha$，"
       r"它在嵌套阈值族 $\Lambda$ 上算经验风险 $\hat{R}(\lambda)$，按"
       r"$\lambda^{\star}=\inf\{\lambda\in\Lambda:\hat{R}(\lambda)+\sqrt{\ln(1/\delta)/(2n)}\le\alpha\}$"
       r"挑出阈值，其中 $n$ 是校准集大小，$\delta$ 是置信参数。"
       "风险关于阈值单调时可以绕开多重比较校正带来的保守，"
       "不单调时退回逐个阈值检验再做校正[22]。"
       "本文的风险函数取每个窗口上编辑有没有造成损害这个零一指示，"
       r"被标定的对象是屏蔽里的结构失真阈值 $\tau$。")

    # 2.3
    rw("给定含缺陷的时序语料",
       r"给定一份含噪的时序语料 $\mathcal{D}$，其中每个窗口 $x$ 是定长的单通道序列，"
       r"再给定动作空间 $\mathcal{A}$ 与总探测预算 $B$，"
       r"治理要做的是学一个策略 $\pi$，为每个窗口挑动作序列，"
       "在把语料对目标模型的效用提上去的同时不破坏窗口原有的时序结构，"
       "证据不够时则什么也不做。"
       "把这个过程写成受约束的马尔可夫决策过程[17]，五个要素定义如下。")

    rw("这一问题设定有三个特征决定了方法的设计",
       "这个设定有三处特点，方法的形状基本是被它们逼出来的。")

    rw("（2）试错的代价落在不可恢复的对象上",
       "（2）试错的代价落在改不回来的东西上。"
       "语料改过之后没法还原，这一点和可以重置的仿真环境不同。"
       "探索要是直接作用在原数据上，学习过程本身就是破坏过程。"
       "把执行和提交拆开，是让学习变得可行的前提。")

    d.save(PATH)
    d2 = docx.Document(PATH)
    print(f"2.1 与 2.3 完成，公式 {before} -> {count_math(d2)}")


if __name__ == "__main__":
    main()
