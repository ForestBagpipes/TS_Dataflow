# -*- coding: utf-8 -*-
"""Rewrite the introduction following the reference paper's skeleton.

That skeleton runs背景, 定义, 技术方向, 问题, 梳理, 局限分点, 挑战, 方法, 贡献.
Each paragraph does one job and sets up the next, and the connectives are
reasons rather than ordinals. Three things were missing here. There was no
paragraph introducing what data curation is and what question it answers, the
limitations were argued in one block instead of enumerated with examples, and
the challenge paragraph had been folded into the method paragraph so the reader
never learned why the thing is hard before being told how it is done.
"""
import sys
sys.path.insert(0, "tools")
import docx
from docx_rewrite import (count_math, delete_paragraph, find_paragraph,
                          insert_after, set_paragraph_md)

PATH = "docx/胡宏彬-进度文档-20260820.docx"


def main():
    d = docx.Document(PATH)
    before = count_math(d)
    drop = []

    def rw(prefix, md):
        set_paragraph_md(d.paragraphs[find_paragraph(d, prefix)], md)

    # 段一 背景与定义，一段漏斗收到需求
    rw("设备状态监测、电力负荷调度与金融风险评估正在把预测任务交给同一类模型",
       "人工智能领域正在经历一场重心转移，研究者关注的对象从模型架构逐渐移向数据本身。"
       "时序分析领域的这一转移体现得尤为明显。"
       "设备状态监测、电力负荷调度与金融风险评估此前各自训练专用模型，"
       "如今则把预测任务交给同一类时序基础模型，"
       "这类模型在大规模多来源语料上预训练，再迁移到预测、分类与插补等下游任务[1-4]。"
       "随着模型结构收敛到少数几种成熟设计，能力的上限不再由架构决定，"
       "而是取决于投喂进去的语料。"
       "一份可用于预训练的时序语料通常需要在两个方面达标，"
       "一是结构完备，语料应当覆盖设备或市场实际出现过的各种运行模态，"
       "既包括常规工况，也包括状态切换与罕见但真实发生的极端事件，"
       "模型只有在训练中见过这些形态，部署时才可能认得出来。"
       "二是信噪可控，传感器故障、通信丢包与记录错误带来的失真应当尽量少，"
       "这类失真一旦留在语料里就会被模型当作规律学习。"
       "随着物联网设备与交易系统的普及，可获取的时序记录正以指数速率增长，"
       "而规模的增长同时带来了噪声的累积，"
       "原始采集在上述两方面都得不到保证。"
       "如何从大规模含噪的时序记录中整备出高质量语料，"
       "已成为时序基础模型训练链条中一个绕不开的环节。")

    # 段二 本领域技术方向
    rw("一份可用于预训练的时序语料应当同时满足两个条件",
       "为应对这一需要，时序数据治理作为高质量数据准备的重要分支，近年来受到越来越多的关注[5,6]。"
       "治理技术要回答的是哪些数据需要处理以及应当怎样处理，"
       "它或者按既定规则改写可疑的取值，或者估计每个样本对模型的贡献并据此取舍，"
       "目标都是在控制人工投入的前提下提高语料的可用程度。"
       "与逐条人工审核相比，自动化治理让十万量级窗口的整备成为可能，"
       "也让整备标准在不同批次之间保持一致。")

    # 段三 本文关心的问题
    rw("把这两个条件同时做到，难点在于判断某一次具体的修改该不该做",
       "然而，时序数据的一个特点让治理变得远比通用数据困难，"
       "那就是一次修改是否有害往往取决于被修改的那段数据，而不取决于修改本身。"
       "以电力变压器监测语料为例，设备检修期间的一段水平漂移应当处理，"
       "它会让模型把异常水平当作正常基线，"
       "在随后的正常时段给出系统性偏高的预测，误差可以持续到漂移结束很久之后。"
       "而一次真实发生的负荷突增在波形上与故障尖峰几乎一致，"
       "它却正是结构完备性所依赖的那部分数据，削掉它等于删去模型最需要学习的稀有模态。"
       "两者的区别不在算子也不在参数，"
       "同一个去尖峰算子带同样的参数施加下去，前者是修复，后者是破坏，"
       "结果取决于那段数据里原本承载着什么。"
       "这意味着依靠事先写定的规则去判断一次修改是否值得做，在原理上就不可靠。")

    # 段四 梳理总起
    rw("围绕这一困难，已有工作沿三条路线展开",
       "为系统评估现有方法在这一问题上的适用性，本文对时序数据治理与选择技术做了梳理与分类，见表 1。"
       "按照判定发生的时刻与判定所依据的信息，当前工作大致可分为三类。")

    # 三条路线保持，微调措辞使之与总起衔接
    rw("修复式清洗依靠先验规则定生死",
       "（1）修复式清洗依靠先验约束判定取值是否合法。"
       "SCREEN[5]假设相邻时间步的取值变化存在上下界，把违反约束的点拉回最近的合法取值，"
       "IMR[6]改用自回归模型逐点估计并迭代收敛，"
       "MTCSC[8]把速度约束与聚类结合以利用维度之间的相关性。"
       "这一类方法的判定发生在选定算子的那一刻，规则一旦给出，改写就直接作用在原值上。")
    rw("打分式选择只估值不动手",
       "（2）打分式选择依据样本对模型的估计贡献判定去留。"
       "Data Shapley[9]用合作博弈中的 Shapley 值刻画样本价值，"
       "Data-OOB[10]借助袋外估计把这一估计的开销降到可接受范围，"
       "TimeInf[11]把影响函数定义在时间块上以保留时序结构，"
       "TSRating[12]则用大模型的成对比较蒸馏出轻量评分器。"
       "这一类方法产出分数与排序，语料按分数取舍而不被改写。")
    rw("学习式编排让策略从反馈中改进",
       "（3）学习式编排依据下游反馈的期望回报判定该调用哪个算子。"
       "清洗智能体[13]用分层策略决定在什么窗口上施加什么操作，"
       "并把下游误差的变化作为回报，策略可以随训练逐步变好。"
       "这一类方法把判定交给了学习到的价值估计，而算子在训练过程中立即生效。")

    d.save(PATH)
    d2 = docx.Document(PATH)
    print(f"引言前四步完成，公式 {before} -> {count_math(d2)}")


if __name__ == "__main__":
    main()
