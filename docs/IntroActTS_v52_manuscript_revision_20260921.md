# v52 论文修订记录

活动稿件为 `latex/IntroActTS_20260921_v52.tex` 和同名 PDF。原 v51 入口、PDF、脚本与实验图保存在 `archive/paper_20260921/v51/`。未修改原始实验结果，未运行训练、预测或 bootstrap 重采样。

## 证据与正文

本轮接入作者报告的服务器提交 e78f206 对应 21 份结果。可移植摘要及全部源文件 SHA-256 位于 `latex/figure/data/v52_evidence.json`。本机已逐份核对哈希，脚本直接读取存储的指标和区间。

正文消融表改用机制级 A2 Pooled action utilities。FULL 与该对照的三骨干平均 MASE 为 1.4366 与 1.4470，只有 Bolt 的名义区间排除零。该对照同时改变局部检索与全局先验的组合，因此正文不把它写成对所有机制因素的独立因果识别。

原 A2 删干预特征结果仍保留在附录，正文明确其 MASE 1.4185 优于 FULL。附录报告原 A2 的逐骨干差值与区间，包含 Bolt 上有利于删特征的区间。线性估计器的竞争力也予以保留。

门控对照加入正文三面板图，展示 FULL 减对照的误差差值与名义区间、实际 TEST 干预率、有害损失。阈值只在开发块匹配频率，不能称测试集严格同频率。对随机门控的误差优势在三个骨干上均获名义区间支持，但随机门控的有害损失均更低。对 mean-only 的优势仅 TimesFM 区间排除零，对 linear 仅 Bolt 排除零。随机控制只有一个种子，区间不包含门控随机种子变异。mean-only 和 linear 还会改变推荐动作，并非仅替换接受阈值。

稳定性表原有 1.378 等值保留为双 horizon 汇总，修正正文误标 H=96 的描述和差值方向箭头。附录另披露作者报告的 H96-only 数值，避免混淆统计范围。

摘要、引言贡献陈述、消融解释和结论同步收窄。修正邻域距离误差的非单调性、离策略估计必需 propensity correction 的过强说法，以及部分与事后评估边界冲突的预先冻结表述。没有把未运行的独立确认、parent 聚合邻域、NaN 特征运行级修复或延迟重测写成已完成。

## 绘图与排版

参考用户提供的两张样图，采用同尺寸面板、紧凑共享图例、向内刻度、统一边框以及颜色与形状的共同编码。保留用户要求的冷色和竖柱，无背景网格。FULL 使用深蓝，其他对照使用浅蓝、青绿与紫色。实际量纲不同的轴保留合理的独立步长，不强行使用相同数值范围。

实验图由 Python 直接读取原始表格 CSV 或 v52 记录摘要生成矢量 EPS。图1和图2继续从作者 BMP 无损封装，像素逐一相同。主实验及消融保留正文表格，门控图替代正文原有汇总伤害图的位置，后者完整保留于附录。

## 检查与范围

正文结论截至第9页，AI Use Statement 紧随结论。编译没有未定义引用或溢出框。原有40条实际引用及文献库哈希仍与上一轮核查记录一致，本轮没有重新执行 Scholar/DBLP 检索。旧 CSV 与来源表格的显示精度核对通过，21份新输入哈希核对通过。PDF 和门控图已渲染检查标签、对齐及页内排版。

这些检查只确认文稿、资产与存储结果的一致性，不等于独立复现或接收保证。正文保留 TEST 已被设计过程接触的事实。
