# v4.3.1-r2 冻结实施计划

2026-09-15 Asia/Shanghai，接续 6f0542b，开始时工作区干净。用户本轮未给新的截止时刻；按可验证完整交付推进。实际资源16 CPU、47GiB RAM（约44GiB可用）、RTX4090报告49140MiB、117GiB磁盘余量，无活动GPU计算。三个环境与两家族checkpoint复用，不改代理或驱动。每进程BLAS1线程，单重GPU、最多两项CPU计算，其他agent主要只读审计。

## 核心冻结（新r2 DEV结果前）

- 目标变量/数据原始时间/L512/H96、H192/MASE/五臂和目标checkpoint不变，不做预测后修正。
- 原三来源全部保留：ETTm1、Solar、US_Term_Structure；旧DEV26 parent/156变体永久是已使用DEV。严格704跨度不重叠的train上限110，实际59/44/7全部用尽，增加max_origins=64不会增加支持。
- 仅一套普通CART规则：depth3、min_samples_leaf96、随机种子101、训练后实际每叶至少16独立parent。三个独立可见状态mask/history/both；both是两工具组合。所有未取得工具整组特征不可进入状态模型。无证据使用各家族训练选定固定参照；完整target观测直接KEEP。
- 本轮不做配置筛选和剪枝搜索，合并原T_fit54+T_gate21为75个终态拟合parent；原T_check17与T_acq18不变。增加的是拟合利用率，不称新增原始数据。状态策略和固定参照仅用75父组，源码/整个状态模型集合hash冻结后才生成18父组的获取标签。
- 25/50/100%学习曲线仅在上述75父组中按source及原始时间稳定分层抽取嵌套子集，目标18/37/75（来源取整会在清单列明）；相同T_check17，检查集不进入任何拟合或参照选择。100%始终是主候选，不根据检查曲线另选比例。
- 获取器沿旧depth2/每叶16parent，动作mask/history/both或STOP；一次获取决策，组合内部2工具完整计费。lambda沿登记0及0.1×train任务差绝对值中位数/正工具成本中位数，在T_acq按parent LOPO选择。不得以负oracle差替代同冻结策略实际损失差。
- 预算原样low=0.8140623268639832秒/high=3.5秒，准入检查完整branch。主效果假设要求胜固定强参照与固定同信息CART；成本替代假设预登记误差容忍为0 MASE，即不接受点估计任务退步来换成本，配对区间一并报告，不构成理论保证。
- 固定取证方案由训练内各状态损失与既有预算确定并冻结，完整agent和其固定取证对照共享状态策略、特征、候选、监督、预算。另报告全部两工具普通CART以保留旧可比参照；不通过裁减TATO的原生空间获得优势。
- 金融优先核实Oil及USTS。Oil若身份/原时间匹配通过，新增DEV只能使用既有dev边界内唯一完整parent，预测与全候选冻结后才首次读取future并单独封存首次结果；三来源旧表与扩展表各自重算，不和旧1.157005跨集合混比。Crypto既有dev长度不足，不能移边界纳入。未确认日历的自然NaN不冒称应有交易缺失，金融身份不完整明确缺项。

## 并行归属和实际队列

A审计agent独占r2_stop_audit/r2_audit脚本：旧STOP逐窗归因、支持审计和独立复核。B数据agent独占r2_finance脚本/financial_audit：原始金融身份、时间与缺口。C方法agent独占src/introact_ts/v431_r2/{policy,acquisition}.py与对应tests。root负责数据/缓存、GPU生产、训练评价、在线回放、共同表、论文、审阅包及Git。旧v43/v431模型与worker不改写。

GPU队列首项：5010个缺失TimesFM请求（旧train当前预测+所有target-horizon历史预测），按原始输入、mask、模型与源码hash核验，单次常驻模型，预计5–12分钟（以首批实际速度更新）。其次仅必要Oil候选/证据、两家族预测/TATO，再真实在线自然获取/STOP/低预算/失败回退。金融身份审计不阻止前一批。总体首版预计约45–90分钟，未知外部来源响应可能延后金融细节，不保证时间。

每批保存命令、状态、PID、输入/模型/代码hash、raw预测、失败和成本；只补相关回归。工程成功和研究晋升分开。calibration/test封存，PICS_joint_relabel不变，RED不更新DOCX已验证成果。
