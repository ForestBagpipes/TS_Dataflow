# P2 首批真实方法实验：完成，A5 增量未成立

2026-09-14 21:16:24–21:20:35（UTC+8），耗时 251.386 秒。51 项 CPU 语义测试通过后，运行冻结 H96/H192、L512、seed101、通道0 的三个 dev 来源；ETTm1 / Solar / USTS 分别 14 / 11 / 1 个基础 parent。两 horizon 和 raw / target-block10% / shared-block10% 产生156个变体，不能算成156个独立时间样本。

本批 A0 native/ffill、A2、A3、A4两版本、A5 共1092份任务标签全部完成。512次真实TS-ICL插补、580次去重后的Bolt预测；独立复核原始quantiles、预测、输入hash、所有臂观测值、共同future/mask、train MASE尺度、候选alias、A5静态eta及开发oracle通过。calibration/test读取为0；post-hoc独立验证没有新增future读取。实际forecast去重只发生在同episode、同horizon与完整输入相同的候选之间。

| 臂 | 来源宏平均 MASE | 来源宏平均 MAE | task harm（变体计数） |
|---|---:|---:|---:|
| A0_NATIVE | 1.258454 | 3.799927 | 0/156 |
| A0_FFILL | 1.430629 | 4.577188 | 68/156 |
| A2_SINGLE | 1.157005 | 3.266175 | 31/156 |
| A3_COV | 1.313983 | 4.050635 | 54/156 |
| A4_RIDGE_CONTEXT | 1.214538 | 3.538100 | 19/156 |
| A4_RIDGE_FULL | 1.218877 | 3.559877 | 19/156 |
| A5_STATIC | 1.157187 | 3.266634 | 31/156 |
| A9_ORACLE_AVAILABLE | 1.093197 | 3.049772 | 0/156 |

A2 相对 native KEEP 的宏平均MASE改善 8.06%；A5比A2略差 0.0157%。没有可靠确认性置信区间：只有3来源，USTS仅1个基础parent，且A4全历史窗口共享dev历史。宏平均按每来源内horizon/缺陷等权，最后来源等权；单一来源或156个变体不能虚增独立支持。A1历史PICS/v39D新区间适配未完成，原生多变量任务模型未运行；本表不等于完整A0–A5/全污染矩阵，也不支持相对incumbent晋升。

## 实际归因

- ETTm1：A2整体MASE 1.058308，差于KEEP 0.997525；Solar：A2为1.402176，优于KEEP 1.751732；USTS：A2为1.010530，略优于KEEP 1.026104，但只有1个parent。不能把A2整体改善说成各来源统一有效。
- A5只在ETTm1的4个parent（8个horizon变体）选择eta=0.5，其余为eta0或支持不足回A2；这8个变体中未来任务2个改善、6个退步。后验真缺口重建恰好6个改善、2个退步：本批进一步展示重建变好不能代替预测任务收益。
- 额外信息的开发上界仍有空间，但来自简单强对照：target-only候选oracle MASE 1.113913，加入官方cov与context ridge降至1.094080；再加入A5仍为1.094080，**A5在已有强候选集合上的oracle增量为0**。额外全合法历史ridge为1.093197。ETTm1和Solar均存在简单跨通道候选的开发线索，尚不能据此称残差创新成立或部署选择器已有效。
- A5的54个共享缺失支持不足案例明确回A2，另2个缺少三个observed伪块案例回A2；50个无缺失输入保持原值，42个受支持案例选eta0，8个选eta0.5。真实模型没有失败填零或丢行。

所有候选输出在解码future之前冻结。独立归因脚本只在评价阶段读取已授权dev原始context，用于合成隐藏位置的重建诊断；自然缺失无clean，不能造repair标签。KEEP未填位置的repair MAE保持null，明确unfilled，不当0分。原始文件保存完整支持量与实际缺口误差。

## 成本与后续边界

六个worker加载合计 6.948 秒，推断合计 41.267 秒，worker墙钟合计 75.649 秒；最大GPU分配1,899,153,920字节。总墙钟251.386秒含原始行读取、CPU ridge、序列化和评价；不能只拿GPU推断秒数外推整个矩阵。GPU空闲的CPU阶段不是下载卡住。

按相同首批负载粗略外推每1000个episode约 1933.7 秒（含20%余量）；或每1000个基础parent约 11602.4 秒。此值受来源通道数、全历史长度和成本占比影响，仅作相同负载预算，不适用于新大来源、适配或租费。

本阶段判定：实验工程完成；A5静态残差增量未成立，保持诊断分支。PICS_joint_relabel仍为incumbent，不训练A8，不打开calibration/test，不将本批写成DOCX已验证方法进展。下一步先补A1可部署适配与更有支持量的第三来源，扩5%/点缺失等预注册开发条件，复核简单跨通道候选的可部署选择是否具有稳定任务收益；A5无上界增量时不继续扩大其复杂度。

可复核证据：`docs/v43_p2_evidence_20260914.json`。原始run：`results/v43/20260914T131623.999847Z-p2/`；全部日志/资源：`logs/v43/20260914T131623.999847Z-p2/`。独立入口：`scripts/verify_v43_p2.py`、`scripts/analyze_v43_p2.py`。任务与监控均已正常结束，终态exit_code=0，未宣称还在训练。


### 2026-09-14 后续诊断修正

协变量不足的54个变体应拆为52 shared-block + 2 USTS raw，另2 USTS target-block缺三个伪块。A5无oracle增量仅指静态选择后的候选；全部eta补齐后强五臂oracle从1.094080变1.093753。26个比静态更好的替代含20个非零修正和6个eta0回退。完整代码复核见[v43_agent_report_20260914.md](v43_agent_report_20260914.md)，原数值与失败保留。
