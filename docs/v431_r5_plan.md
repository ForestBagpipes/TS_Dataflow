# v4.3.1-r5 执行前登记

2026-09-16 Asia/Shanghai 01:48接管，实际HEAD ac6aaa6cf49cd1b61faa12afece19eb0548af952，初始工作区干净。r4及以前结果不修改，旧DEV/check/acq/金融均已用，不是确认。calibration/test封存；主实验数据和基线准备与开发并行，只有用户列明四项准入同时成立才进入下一阶段。

固定五臂、两家族真实checkpoint、L512/H96/H192与原报告MASE。学习和几何使用r4 origin_scale；未来有效评分mask部署不可知，所以主实验几何一律unknown集合/max lead距离，不读取未来NaN位置收紧约束。known固定权重仅数学单测及事先固定完整评分轨道，不用已看未来mask伪装known。

新增当前任务查询结果类型，只有实际查询才揭示输入/预测/身份/成本。参考动作来自fit免费深1成本敏感树（每可执行学习叶16parent）；fit元学习参考按同source完整读取范围forward/purge交叉拟合，过去支持不足固定协议TS-ICL，不借用本parent或不可比较跨源时钟。最终参考全fit训练一次，gate不事后按DEV选参考冠军。

免费字段沿r4可见13项，另增加由当时mask/时间几何直接重建的目标H历史协变量覆盖，缺失有类型。该计算不调用历史模型。响应固定七项，预查询先验为相同七统计应用于已执行p0相对零向量；全部按origin尺度。ridge共享斜率+动作截距，标准化/缺失填充值仅fit，alpha仅0.1/1/10。训练监督为真实当前MAE差/originS，未来标签不在工具结果内。

评分对照FREE/HISTORY/CURRENT沿同ridge实现与正则搜索，容量差异和信息/成本分开注明。history的七响应取已执行long历史两臂预测差，付整个历史工具费用，不能称当前L512部署证据。

几何模式raw/single/pair/full；full为NumPy Frank–Wolfe，tol1e-8、max_iter2048、每调用时间上限0.05秒。非超时迭代上限可返回可行近似和最终gap，不能写精确投影；超时/数值异常保留前有效动作并STOP，所有已发生费用保留。pair独立SLSQP及证书；参考坐标严格0。最终只用一个已执行版本预测，不混合。

选择性规则mu_a−m−lambda*预计增量费用，lambda只0/0.01/0.1由gate选，平局低费/稳定动作序。最多参考+两候选；固定顺序为五臂已有稳定次序去掉参考，固定数量与选择性均最多3，另全部5高成本对照。费用沿已审计常驻账本，输入canonical重复只有物化完成后可省已执行相同预测，不能先偷看未查候选hash。预算low0.8140623268639832/high3.5秒。按实际发生诊断/物化/预测/求解/输出扣预算，不在最终排名重复扣已花费用。每个新增试运行分支另预留求解上限0.05秒与评分/记账0.001秒，所有评分消融采用同准入预留；另预留输出0.001秒，预测不足也实际返回参考并记录未满足；不填零。

先冻结reference/scorer/prior后gate选择alpha和lambda（每模式仅3候选，不做笛卡尔组合扩展）；再一次DEV共同矩阵。固定轨迹评分消融共享查询集合和raw critic；端到端轨迹另列。单臂/成对/full中查询集合少于3不同预测单列，不证明高阶贡献。实际几何耗时不能混成TSFM调用。

诊断oracle计完整查询及执行费，仅TRAIN分析；共同表source→parent→variant等权，source内时间parent块2000 bootstrap/seed101描述区间。强简单流程从TRAIN按原规则冻结，R2不同信息明确。两个家族都未胜冻结简单对照、或同信息full无增量时，停止扩大r5搜索，保留负结果并继续基线/独立数据准备。

服务器实测GPU空闲、47GiB RAM、110GiB盘；当前nproc报告1（与r4历史16不同），CPU轻量并行但BLAS1，后续吞吐以实际测量。现有三个环境及两骨干健康，不重装。root唯一GPU队列，服务持原gpu.lock，外层独立队列锁，避免r4嵌套锁失败。

并行归属：root数据/运行时/统一训练评估/论文交付；r5_geometry仅求解器及数学单测；r5_scoring仅免费参考与ridge；r5_baselines仅主实验元数据、官方TATO职责和文献。全部命令、配置、来源hash、失败、费用保留；关键批次在聊天给完整数字，不只文件链接。
