请接管 IntroAct-TS，基于已完成的 v4.3.1-r3 实现下一轮。以下内容可独立执行。本轮目标是尽快得到值得继续验证的完整候选和共同实验表，不能将规划写成已实现，将开发点估计写成 SOTA，或保证 ICLR 录用。

一、固定目标与起点

方向固定为面向冻结 TSFM 的数据治理 agent，在相同基准、信息、监督和预算下提升最终预测表现，服务 ICLR 2027 投稿及大模型数据治理岗位。保留有效金融观测和真实跳变，完整输入允许 KEEP，不改为交易系统、预测器路由、合成数据项目或 TSFM 参数训练。

问题定义是在未来结果尚不可见、输入覆盖可能不完整且验证计算有限时，选择有效的输入治理动作及必要验证步骤。备份与回退不作为核心创新。

暂定题目为面向冻结时间序列基础模型的任务感知数据治理智能体。
英文为 IntroAct-TS: A Task-Aware Data Governance Agent for Frozen Time Series Foundation Models。
金融继续作为重点应用，但现有两个金融 parent 不足以证明金融有效性。

起点提交为 a5d395cf9cd369941be51c6fbaf290971d3144b8。先核对实际 HEAD、工作区、AGENTS.md 和已有进程。若有后续合法提交，保留并沿其增量实现，不重置分支或覆盖他人修改。

先读：
docs/v431_r3_report.md
docs/v431_r3_main_table.md
docs/v431_r3_code_audit.md
docs/v431_r3_verification.md
docs/novelty_matrix.md
实际配置、统计和成本账本

审阅包为 results/v431-r3/review-20260914T180321Z.tar.gz。
文件缺失时沿仓库真实入口查找，记录路径映射，不杜撰已有模块或命令。

已知结果必须保留。共同 DEV 为 26 parent、156 相关变体。
Bolt 固定参照 1.157005、r3 agent 1.156351、DIRECT_control 1.146643、R2_EXISTING_CART 1.136486。
TimesFM 固定参照 1.096135、r3 agent 1.102669、CART_H 1.069398、R2_EXISTING_CART 1.069086。
R2 的证据不同于 r3 control，不能冒充同信息消融。全部只是旧 DEV 点估计。

r3 Bolt 主动与固定取证 156/156 相同，TimesFM 主动退步。
有效 fit/gate/acq 为 51/20/16，获取 LOPO 每折 15，低于 16 支持门槛，获取树退化为常数。
110 个登记 parent 不等于获取器有 110 个有效样本。
旧 STOP 修复已完成，不重复诊断成 STOP 必须等于 TS-ICL。

calibration/test 继续封存，PICS_joint_relabel 保留原协议 incumbent。
旧 check、acq、新位置检查和金融附表均已查看，不得改称新确认集。

二、先完成有限的代码与账本复核

沿真实 CLI 追踪五臂、数据前缀、特征尺度、策略训练、工具执行、回退和最终预测。

五臂准确身份为 Native KEEP、FFILL、单变量 TS-ICL、多变量 TS-ICL、context ridge。没有预测后残差臂。
A4 unsupported 回 Native 时保留 alias 原因、真实动作和费用。
完整有效 target 直接 KEEP，所有有效观测不覆盖。

修复 v43/p2.py 继承的整 TRAIN 尺度进入内部早期特征的问题。

外层报告 MASE 分母继续按已登记外层 TRAIN 冻结。
训练窗口自身特征及训练损失尺度只用该 origin 可见的输入。
学习器标准化和分位阈值只拟合 fit。
历史 probe 的输入预处理只能使用其历史 origin 前的合法输入。
请求时已知的历史试验结果可以在请求 origin 的合法尺度下汇总，但不得反过来参与历史预测输入构造。

具体正尺度按当前 512 点输入中可见的季节差分对计算，最少 16 对。
不足时按登记的 lag-1 有效差分，再按可见值 MAD 的固定顺序回退，最后用固定数值下限防止除零。
记录实际分支和支持数。
不得补读 clean 隐藏点、额外历史或未来值。
外层评分尺度和学习尺度分别命名、存储并标明单位。

逐项给出受影响依赖。
只改变后处理特征与学习标签时，复用已经有效的原始预测与 MAE。
影响模型输入时，只重跑受影响后代及对应基线。
新旧缓存按代码、输入、mask、模型和尺度版本区分，不能用旧缓存掩盖新输入。
旧结果不删除，新协议修复表单独生成。

在服务器用现有账本核算：
合法独特动作 oracle。
动作成本可行 oracle。
冻结完整策略库的可行最佳路径。

它们仅作诊断。
完整路径必须包含实际证据与最终预测费用，不能凭未来真值免费选择工具或叶动作。

分解候选无效或重复、预算排除、证据排序错误、取证选择错误各自的损失。
支持数同时报告 parent 与相关变体，不能把输出文件数量当作独立样本。

三、本轮只实现 r4-JOINT 一个候选

保留五臂、ProbeSpec/Result、H32/H/control 工具、canonical 去重、观测保护、费用与在线运行时。
不增加算子，不扩 λ 网格，不增加上下文，不做 LLM 在线决策或深度强化学习。
κ 和 z 不再进入主候选，旧响应方法保留作负结果对照。

用 TRAIN 窗口各动作的最终预测损失及真实费用，直接优化整条可执行路径。

目标为按 source、parent、variant 逐层配权的平均最终任务损失差，加原登记 λ 乘完整部署成本差。
参照为训练内选定固定动作，所有单位写入配置。
未来标签只进入 TRAIN 监督和离线评估，不能作为部署特征。

候选策略语法限定为：
一次可选免费特征二分。
每个分支可以直接提交一个合法治理动作，或调用 H32/H/control 中一个工具组合。
取证后至多一次证据特征二分，再提交治理动作。
每次请求最多一次获取决策，组合内真实模型调用全部计费。

免费特征限于当前可见输入统计、H、缺口几何、合法支持和剩余预算。
证据特征限于选中工具已经取得的历史损失、原始分歧和覆盖信息。
构造候选和模型推理产生的特征均按实际成本处理。
冻结字段白名单，未获得证据用带类型的缺失状态，不能伪造为实测零。

每个特征只用 fit 内 25%、50%、75% 分位阈值并去重。
每个学习终态至少覆盖 16 个不同有效 fit parent。
对有限策略语法做精确枚举或等价动态规划，叶节点通过所属训练组的总任务损失选择动作，输出实际搜索策略数。
禁止按每条记录的真实未来单独选动作后称为学到策略。

整条策略只在原 fit 内联合训练。
原 gate 只选择是否启用免费二分及原已登记 λ，不增加网络、树深和超参数网格。
平局优先低费用、少节点、固定参照。

旧 acq 不再负责拟合第二个获取器，改为冻结后的回归检查。
记录本轮角色用途变化，不能称独立确认。
若合法支持无法产生分裂，如实返回固定策略，不降低门槛强造调用。

运行时显式实现：
decide(visible_state, acquired_evidence, remaining_budget)

不能把带有未来标签和隐藏工具结果的离线整行传入策略。
提交动作、停止取证、预算回退分别记录。
预算不足或工具失败走训练内登记的可行回退，完整 target 直接 KEEP。
预算连最终预测都放不下时记录总预算未满足，不能将零取证写成零费用。

建议新增或映射至现有目录的模块为：
origin_scale
trajectory_dataset
joint_policy
branch_cost

先写真实入口映射，不重写整个工程。

四、一次形成共同结果和必要对照

协议修复后的主表至少包含：
KEEP 与固定五臂。
训练固定参照。
r3 冻结版及修复后重训版。
R2_EXISTING_CART。
DIRECT_control 与 CART_H。
固定取证 CART/direct。
通用成本敏感联合策略。
r4-JOINT。
已有 TATO 适配。

共享实际输入、评分 mask、来源权重、模型版本和预算。
信息不同的完整方法明确标注，不混作同证据消融。

用同一批账本一起检验：
联合对分阶段训练。
可选对固定取证。
有证据对仅免费特征。
实际任务损失目标对普通最优臂分类。

对照匹配容量、阈值数量、监督和支持。
分阶段获取若仍因支持不足退化，明确标注，不能把击败退化版本当作击败有效的分阶段学习。

通用成本敏感联合树是必要对照。
如果它与 r4 实质相同，合并为一个方法并承认采用通用方法，不能换名称制造创新或自我比较。
第一轮不等某个模块显著后才运行其他已登记对照。

学习与家族适配规则在两家族一致，允许分别用各自 TRAIN 学参数。
禁止看到本表后指定 Bolt 用一种算法、TimesFM 用另一种算法，再称统一方法胜出。
分别训练两个家族不等于策略零样本跨家族迁移。

旧 26 parent DEV 仅用于候选筛查及回归。
报告配对 source/parent 块统计、各来源结果、尾部退步、全部失败、不支持和超预算。
保留完整分母，机制子集另列。
单一 USTS parent 和反复使用 DEV 的限制必须明示。

近期比较首先补齐 TATO 官方配置、原生空间、训练信息、费用和失败记录。
8 trial 是短预算适配，不能写成官方完整复现。
官方协议成绩与当前受限治理协议成绩分表，不混用指标。

并行核对最新公开 TSFM 的版本和适配条件，至少登记 TimesFM-3 与 Chronos-2。
实际执行前检查许可、依赖、显存和模型原生缺失能力。
不用升级骨干掩盖治理方法没有增益。

五、金融与预算同步处理

金融已核为 Brent Europe 现货报价和 Kim–Wright 拟合远期利率，不再把二者都写成成交价格。
现有金融附表只有两个 parent，且与旧 DEV 部分重叠，不是独立确认。

必须保留 Bolt KEEP 2.820959 这个强对照，不能只比较固定 TS-ICL 3.069683。
TimesFM r3 12 窗有 8 窗超预算，不算同预算改善。

核查 CART_H 与 CART_H32 汇总 MASE 同为 4.890237、超预算数却为 0/6 的具体原因。
比较逐窗预测 hash、模型调用、准备及初始化耗时，不从相同均值推断相同预测。

预算保持：
low 0.8140623268639832 秒。
high 3.5 秒。
沿用原常驻模型口径。

成本模型只能用合法 TRAIN 性能测量拟合，按家族、长度、H、缺口和执行分支核对尾部费用，预留最终治理与预测开销。
自然单请求实测用于验收，批量摊销不能替代单请求延迟。
冷启动、热请求、完整进程分别报告，不把预测费用或失败费用删除。

训练、历史探针收集、官方基线搜索、模型加载和部署费用分别记账。
缓存减少本轮重算，不使历史计算变成免费。
固定原生方法和 TATO 也按实际成本核算，缺预算 flag 不等于预算内。

补金融自然 NaN 的日历、发布频率、真实缺失及未知原因元数据。
缺少历史 vintage 或发布时间时保留未知，不声称严格 point-in-time。
完整观测、受控删除、可验证自然缺口分别报告。
已有身份核验不重复做成新进展。
不要将真实跳变平滑成错误数据。

可并行登记新的独立来源和时间块，以及官方基准实验协议。
只能根据元数据、可用性和许可选择，不能看候选胜率筛选。
现有 calibration/test 不解封，不能从中挑样本救开发结果。

六、服务器资源与执行节奏

全部训练、模型推理、测试、统计重算和下载在服务器进行。
本地仅阅读、编辑、SSH 调度、同步和文件校验。
复用已有健康环境，固定依赖及模型版本。
先查空闲 GPU、CPU、RAM、磁盘和其他进程，再决定并发。

参考 4090 24GB、8 核、16GB RAM：
一次一个重 GPU 任务。
两个家族按批次加载，避免内存交换。
CPU 账本与文档工作按剩余内存推进。

不得终止他人进程、修改驱动、GPU 全局设置或超额占用。
提高吞吐依靠输入去重、合法缓存、常驻模型和合适批处理。
GPU 计时正确同步，但不把同步校验的额外开销混作全部线上固有开销。

接管后首个 30 分钟目标交付实际代码依赖、有效支持、候选收益空间、预算分解和需要重算的清单。
随后争取约 1 小时完成联合策略与共同表，再完成自然在线和金融费用复核。
这是排程目标，按真实资源、缓存失效和进度更新 ETA，不虚报已经开始或完成。

只做覆盖剩余具体风险的测试。
必须检验 origin 与 fit 边界、隐藏标签和未购证据不可访问、unsupported 别名、有效观测不改、完整路径成本及真实自然分支。
受控强制调用与自然策略调用分开。
不要重复无关测试凑数量。

七、晋升、文档与交付

立即新增：
docs/v431_r4_preregister.md
docs/v431_r4_code_map.md
docs/v431_r4_report.md
docs/v431_r4_main_table.md

同步更新 novelty_matrix、论文草稿、主 README、累计研究计划和运行队列。
旧 r3 文档保留，在入口标明响应及主动贡献未得到支持。
本提示词保存为新版本，不能只维护一个旧 MD。

首轮候选必须对比训练内选定的最强可行固定流程和强简单选择器。
仅胜 r3、仅有 oracle 空间、仅有自然调用或测试通过，都不能晋升。
若只改善成本而准确率退步，不能事后定义非劣界限。

若合法 oracle 空间不足，或 r4 仍不能超过强简单对照，停止扩展本轮算法，不增加第五轮特征或打开确认集。
保留负结果和可行工程版本，具体说明任务、信息、动作支持或泛化哪一项受限，并完成基线与独立评估协议工作。

若完整 agent 取得稳定开发改善，再按冻结协议准备独立确认。
方法贡献还须胜同信息的通用成本敏感策略及固定流程。
SOTA 还须近期强方法的公平完整比较。
不能保证录用，不能把这几个阶段写成同时完成。

精简审阅包包含相关真实代码、resolved config、实际执行命令、角色清单、原始成对预测样例、支持与费用、自然轨迹、全部失败、统计代码、提交 SHA 和文件 hash。
不含权重、密码、token 或他人项目。
按既有授权同步当前研究分支并核对远端 SHA，不合并主分支，不覆盖其他工作。

最终直接回答：
实际改了什么。
联合策略是否优于强简单对照。
选择性取证有无增量。
金融 KEEP 和自然缺口比较如何。
总成本是否合规。
哪些近期 baseline 真正执行。
当前能支持哪些论文主张。

附完整表、代码定位、提交和未完成项。
不要将测试数和输出文件数作为研究成功的结论。
