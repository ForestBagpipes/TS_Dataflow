# v4.3.1-r4-JOINT 预登记

2026-09-16 Asia/Shanghai，00:09起接管；起点a5d395cf9cd369941be51c6fbaf290971d3144b8，工作区初始干净。原文见[v431_r4_prompt.md](v431_r4_prompt.md)。本文件是运行前配置，不是结果。r3响应和主动贡献未成立，所有旧表保留。calibration/test封存；旧check/acq/新增位置/金融均为已看开发回归集合。

## 范围与尺度

固定Bolt和TimesFM2.5实际revision、L512/H96/H192、五臂和原始模型输入；只新增r4后处理/学习层，原P2/worker/r3冻结文件不改。原始预测、候选、mask和MAE经hash核验后复用；如治理模型输入变动则对应后代明确重算，不混cache。

`report_scale`保持已登记外层source TRAIN MASE常数。`origin_scale`仅由当前dirty512点目标有限值计算：已有频率的季节差分绝对值均值（至少16有效对），不足则lag1差分（至少16对），再可见值MAD，最后固定下限1e-8。记录所选分支、有效对数和下限触发。当前目标的训练监督为MAE/origin_scale；历史已取得MAE、raw预测分歧用请求origin尺度汇总。历史模型输入构造不使用请求未来信息或这些试验结果。fit标准化/分位仅fit；report_scale不得作为部署特征或训练标签尺度。

原角色54fit/21gate/17check/18acq保持parent和时间边界，主joint只fit54训练；gate只选择是否免费二分和lambda。旧acq仅冻结后回归。外层DEV仍26parent/156相关变体，全部行与失败保留，不改成确认。source→parent→variant逐层等权。

## 一个主候选与有限对照

主r4-JOINT等同此处通用成本敏感联合树，只列一臂，承认采用通用有限策略搜索。语法：最多一个免费特征二分；各分支固定提交或取得H32/H/control；取证后最多一个已得证据二分再固定提交。每个学习终态>=16个有效fit parent，每特征仅fit25/50/75分位去重。精确枚举/DP记录计数，不按逐窗oracle当策略。

免费字段仅当前dirty统计、H、缺口几何、可见支持和预算。证据仅选中工具的历史损失、原始预测分歧、覆盖；主候选不含κ/z。未获得证据使用typed缺失状态。候选值和预测值仅已实测付费后使用。完整有效target直接KEEP；A4不支持记录Native别名、原因和尝试费用。

目标为fit真实当前损失差+lambda完整部署成本差；lambda仅0及旧公式0.1×fit任务差中位绝对值/fit工具正成本中位数，退化仅0。gate跨lambda比较共同的任务损失差，容忍0；不直接比较不同定价目标来选价格。平局低成本→少节点→固定参照。预算low0.8140623268639832秒/high3.5秒，容忍0；成本仅fit TRAIN按family/H/执行分支的max×1.1估计，H桶至少16parent，预留冻结路径实际可达终叶动作中的最大费用。未见成本分桶回训练粗桶，不读取DEV费用选择路径。长短长度与H/分支映射固定；缺口尾部另做核对，不另拟合数据驱动分桶。实际超支完整保留。

必要对照：固定五臂/train固定参照、旧r3冻结完整策略、r3尺度修复后重训、继承R2_EXISTING_CART（不同信息）、修复DIRECT_control/CART_H及固定取证direct/CART、joint仅免费、joint固定取证、同容量最优臂分类目标、分阶段同grammar，以及既有原生TATO8trial。容量、阈值、角色/支持一致的模块对照单列；R2完整原生法不冒同信息消融。

旧r3重训对照按原T_fit的各source时间顺序前2/3 parent拟合终态、后1/3获取监督，旧T_acq不再拟合。其终态沿旧alpha0.1/1/10、旧gate选择规则，另标与主joint角色/搜索差别；若支持不足退化必须报告，不能把击败它写成胜过有效分阶段策略。原r3冻结结果仍完整保留。

## 诊断、统计和金融

先TRAIN独特合法动作oracle、实际动作预算oracle、冻结完整路径库可行oracle，仅诊断；所有路径计工具和最终成本。拆候选重复/无效、预算、证据排序及获取选择限制。正式表每来源、跨度、条件、尾部危害、超支及unsupported完整分母；配对按source内时间parent块，2000次bootstrap/seed101，旧DEV多次使用与USTS单parent限制明示。机制共同支持子集不替换全表。

金融沿已登记观测事件附表12变体/2parent，Brent报价与Kim–Wright拟合远期利率不重命名成交价格；保留BoltKEEP2.820959。无可靠自然缺口则缺项；补日历/发布/未知metadata不看胜率筛选。原H32/H费用差按逐窗hash及原真实调用追溯。

TATO官方配置/原生空间/标签权限/费用与8trial适配分别登记；未运行官方完整协议不填成绩。TimesFM3和Chronos2仅核公开版本、许可/环境/显存/缺失能力，不为升级骨干启动下载或扩模型族。新来源/时间块只登记metadata和许可，不动现有calibration/test。

## 执行与停止

当前16可见CPU/47GiB RAM、GPU4090报告46068MiB、110GiB空闲磁盘，模型进程空闲；单重GPU、BLAS1，root统一启动GPU。CPU实现/账本与官方核查并行。前30分钟争取实际依赖/支持/oracle/预算/重算清单；其后约1小时争取joint主表，按实况更新。模型冻结前不把开发评估注入调参。

必要回归覆盖origin/fit边界、隐藏标签/未购证据、alias、观测保护、完整分支费用与自然在线；强制调用分开。若候选不超强简单策略或oracle不足，停止本轮算法扩张，不起第五轮特征。研究未晋升也完成全表、论文、官方基线/独立评估协议和审阅包。完成检查后以codex-save-local发布当前codex分支，核远端SHA；Windows自动同步未观察到则不宣称已落盘。
