### 2026-09-14 18:48 提交前复核修正

残差特征维度上限现在同时覆盖填充值和缺失指示；原实现先PCA后附加指示可能超过8维。新增40通道带缺失测试，检查实际Ridge输入维度。最终 **40 passed in 0.96s**，日志 `logs/v43/contracts/20260914T104754.619288Z/`。旧39项通过日志与旧等待队列的hash失效状态保留；没有覆盖真实实验或方法结果。

## 2026-09-14 v4.3 新服务器 CPU 契约与真实 pilot 队列

- 新增独立 v43 数据/时间/标签/worker 协议、不可变数组和完整精度 hash；旧源码和历史结果未改写。
- 新增严格嵌套残差、短缺口候选、可见状态选择接口、共同分母与适配 pair hash 检查。尚未训练 scorer/工具模型或适配 TSFM。
- 已实现 `preflight/inventory/data/pilot` CLI；其他完整方案命令仍待实现，不提供空壳成功返回。
- 39 项 CPU 测试通过，0.95秒；两个独立进程生成32-origin数据清单逐字节一致；未读取 future 标签。
- 已对接固定官方源码的两个真实 worker，未称模型验收成功。pilot 实际依赖拦截记录为 `results/v43/20260914T103602.635168Z-pilot`。
- 配置冻结 pilot batch1、L512/H32、51点 context block、通道0；TIME 为共同 benchmark 行索引并记录真实日历/发布延迟限制。正式 H96/H192 不变。
- 一次性 tmux 队列仅等待当前后台环境与模型就绪，不重复安装。每个真实 worker 使用项目 GPU 锁、记录 PID、原始输出、请求身份、模型/env/code hash、耗时及资源。
- 文档同步审计、台账、交接、数据契约、claims 和实验矩阵。没有研究晋升或已验证正向方法结果，不更新进度 DOCX 的成果部分。

---

# 修改记录

每天做了什么，代码与文档分开记。供后续与其他 agent 对话核验。

---

## 2026-08-20

### 文档 胡宏彬-进度文档-20260820.docx

在 0819 版本基础上产出，0818 与 0819 保留。本轮分两批。

**第一批，形式层面**

| 项 | 改动 | 前后 |
|---|---|---|
| 伪代码符号 | 三个算法框共 40 行，表达式转 Word 公式对象，步骤号与关键字与注释保持文字，缩进用不间断空格 | 公式 192 到 259 |
| 算法 3 | 补上原本缺失的 return 行 | 16 行到 17 行 |
| 公式 6 | 决策风险原本只写三项加权，depth 与 cons 无定义。补出各自公式，与 verify.improvement_depth 和 verify.improvement_consistency 一致 | |
| 公式 3 | 补出 d_j 的定义与权重来源 | |
| 公式 11 | 补出 B_left 与代价估计的实际取法 | |
| 方法对比表 | 删除，十行改为正文十条编号句 | 表格 8 到 6 |
| 超参数表 | 删除，八行改为一段文字 | |
| 实验章小节标题 | 4.2.1 到 4.2.8 删除，改为连贯段落 | |
| 数据集表编号 | 表 2 改为表 1 | |
| 重复段落 | 删除实验章两段旧版残留 | |

**第二批，内容与文字层面**

| 项 | 改动 |
|---|---|
| 噪声口径 | 七类缺陷改为两类噪声，随机噪声作用于单通道，系统噪声跨通道同时作用。2.2 节与 4.1.2 节同步 |
| 评测分层 | 六层改为四层，注入层、干净层、稀有层、困难层。稀有与困难取自真实数据而不再合成构造，去掉循环论证风险 |
| 数据集 | 从七项改为六项两类场景。工业取 ETTh1 ETTh2 ETTm1，金融取 TIME 基准的 Crypto US Term Structure Oil Price。参考文献补第 28 条 |
| 基线 | 从十三项收敛到七项，标准是有正式出处、覆盖两个方法族、各自隔离一个变量。Data Shapley 与 TSRating 不单列并说明理由。软惩罚等三种对照降为方法变体 |
| 定理 5 新增 | 标定间隔内保证退化的上界，把共形风险控制与老虎机更新速率联系起来，给出 T_cal 的定量依据。附间隔条件的成立性检验，标注尚未进行 |
| 命题 1 新增 | 软惩罚的损害下界，说明结构约束写成惩罚项时损害率有不可消除的正下界，把与同期工作的区分提到机制层面 |
| 3.1 补充 | 同类校准的生效条件，收益随语料异质性单调增加，在同质语料上趋于零。解释了消融中无差别的原因 |
| 实验八 新增 | 异质性递增语料上检验同类校准与弃权，给出两个方向的判读 |
| 实验九 | 原实验八，敏感性扫描增加 T_cal 一档用于检验定理 5 |
| 文字 | 章首导航句改为由上一步的困难驱动下一步，按参照论文的衔接方式。摘要与结论的性质计数同步更新 |

**理论方案的直觉记录**：`docs/three_weaknesses_plan.md`

**第三批，主线重构**

诊断出的问题是同一个观察在摘要、引言开头、自动化段、两族评述、挑战一与图注中讲了五遍，方法在摘要、引言与贡献列表中复述了三遍，而三重挑战被并列呈现，掩盖了后两者是第一个问题被特定方式回答之后才出现的推论。

确定的主线是，一次修改是否有害取决于被修改的那段数据，不取决于修改本身，因此无法在执行之前判定。本文把判定推迟到执行之后，并把执行放在副本上使判错的动作可以撤回。安全与学习由此出自同一个决定。

| 项 | 改动 |
|---|---|
| 摘要 | 围绕这一个决定重写，不再逐个复述四个模块的实现 |
| 引言开头 | 用变压器的两种情形把困难讲透一次，后文不再重复 |
| 删除 | 劣质数据双重代价一段，与主线无关 |
| 删除 | 自动化不等于串算子一段，与开头重复 |
| 两族评述 | 打分族与清洗族合并为一段，落点统一到两条路线都停在执行之前 |
| 三重挑战 | 改为一个决定与两个随之而来的问题，说明后两者是推论 |
| 方法概述 | 从复述四模块压缩为一次决策的四个环节 |
| 图注 | 只读图，不再重复正文的论证 |
| 贡献 | 五条压到三条，一条框架加两条支撑 |
| 各章首 | 第 1 章落到两条路线停在执行之前，第 4 章落到要检验的几个判断 |
| 结论 | 回到主线，安全与学习出自同一个设计 |

**第四批，引言按七步链条重建**

上一批解决了主线是什么，这一批解决主线怎么讲。按宏大、定义、问题、梳理、局限、引出、贡献七步重排，此前只做到了问题与梳理的一半。

| 步 | 改动 |
|---|---|
| 宏大 | 补场景感，从设备监测与电力调度与金融风控切入，并把语料具象为传感器与交易系统采集的原始记录 |
| 定义 | 新增一段，给出高质量时序语料的两个条件，结构完备与信噪可控，并指出两者彼此纠缠 |
| 问题 | 变压器的两种情形改到定义之后，读者先有判断标准再看操作层面的争议 |
| 梳理 | 恢复表 1，但改为按路线分类而不是按方法逐行列属性，列含判定时刻与判错后果两栏。正文改为三条路线各一段，先总说思想再点名工作 |
| 局限 | 改为三条路线代价互补的对立，动手的没有回头路，有回头路的不动手，学习式两头都想要却把错误落在语料上，共同盲区是判定都在执行之前 |
| 引出 | 图注与模块绑定，两个窗口经取证、沙箱重测、双信号判定得到相反裁决，图 1 成为机制的视觉锚点 |
| 贡献 | 三条改为逐条回应三条路线的代价，判定时刻重合对应形式化，模型读数反向对应双信号判据，探索破坏语料对应副本执行与退化上界 |
| 压缩 | 决定与两个问题三段合并为一段，避免与贡献重复论证 |
| 表号 | 数据集表由表 1 改回表 2 |

**第五批，标题与叙述对齐**

引言与第 3 章章首都已按取证、试执行、判定、学习四个环节叙述，而小节标题仍按模块命名，读者需要自己把环节对上模块。

| 项 | 改动 |
|---|---|
| 3.1 到 3.4 标题 | 环节词置于最前，改为取证、试执行、判定、学习加各自的技术特征。去掉标题末尾的算法二字，算法框自身已带算法一二三的编号 |
| 2.4 整体框架 | 四段的开头改为点出所属环节，与标题和引言呼应。末段的四个模块彼此供给改为四个环节彼此供给 |
| 校验 | 节号引用共六处均正确，无重复段落，无被压平的含公式段落 |

**第六批，引言按参照论文骨架重写**

逐段拆解参照论文的引言后发现三处结构缺失。没有一段介绍数据治理这个技术方向本身回答什么问题，局限被写成一整块论证而不是分点加反例，挑战段被并进了方法段因而读者在知道难在哪里之前就先看到了怎么做。

对齐后的骨架为背景与定义、技术方向、问题、梳理配表、局限分点、挑战、方法、贡献、结构安排，共九段一条线。

| 段 | 改动 |
|---|---|
| 背景与定义 | 从范式转移切入，经时序基模的兴起收到语料质量，再给出结构完备与信噪可控两个条件，末尾收到迫切需求 |
| 技术方向 | 新增，说明时序数据治理回答哪些数据要处理以及怎样处理，以及自动化相对人工审核的价值 |
| 问题 | 变压器两种情形保留，落点改为依靠事先写定的规则判断在原理上就不可靠 |
| 梳理 | 总起句改为按判定时刻与判定依据分类，三条路线各段加编号并统一为先说依据再点名工作 |
| 局限 | 由一段拆为两条分点，每条给出反例。一是判定早于执行选错无法挽回，二是判定依据单一且在受保护数据上读数相反 |
| 挑战 | 新增独立一段，说明判定依据本身有偏且阈值无法事先给定，以及试错代价落在不可恢复对象上 |
| 方法 | 由四个环节的罗列扩写为完整叙述，两处针对某挑战的表述与挑战段一一对应 |
| 贡献 | 三条改四条，每条带上定理编号，新增一条说明实验规划的覆盖范围 |
| 图注 | 去掉与方法段重复的复述，只读图 |
| 摘要 | 与新引言统一口径 |
| 各章过渡 | 章首改为承接上一章留下的问题，第 1 章第 2 章第 3 章第 5 章第 6 章与 3.5 节均重写 |

**第七批，措辞去仿写**

上一批把参照论文的骨架学下来了，同时也把它的措辞抄了过来。开头一句几乎是对方首句的改写，为应对这一需要与经考察两处则近乎照搬。骨架值得学，话要自己说。

改法是从材料本身出发组织语言，开头从人们实际拿这些模型做什么讲起，而不是从对领域趋势的断言讲起。

| 项 | 改动 |
|---|---|
| 开头 | 范式转移改为风电场预测出力、变电站排负荷、交易台看波动这三件具体的事 |
| 技术方向段 | 为应对这一需要这套句式改为围绕这件事已经积累了一批方法 |
| 局限总起 | 经考察改为这三类在各自的设定里都能跑出不错的结果 |
| 挑战段 | 一方面与另一方面改为头一道坎与第二道坎，避开对方的并列句式 |
| 方法段 | 逐条对应两道坎，措辞口语化，去掉针对某挑战这种公文腔 |
| 摘要图注贡献 | 同步换成同一套语气 |
| 结构安排 | 改掉与对方几乎一致的固定表述 |
| 核对 | 范式转移、为应对这一需要、经考察、亟需、一方面、另一方面、重要分支、受到越来越多的关注八处标志性措辞全部为零，与参照论文共有的十字以上片段只剩本文的主要贡献如下这一句学术惯例 |

**第八批，全文语气统一**

引言换成自己的话之后，后面各章仍是书面腔，通篇读下来前松后紧。这一批把同一套语气推到全文。

| 章节 | 改动 |
|---|---|
| 1 章首与三节 | 各节开头不再以类别名起句。约束驱动是这一方向最成熟的思路改为这个方向上最成熟的想法是给数据划一条界，数据估值提供了另一种视角改为还有一类工作干脆不改数据只算每条数据有多大用处 |
| 2.1 | 四把工具的引入改为读者会依次追问的顺序，要知道模型是不是更省力了得有办法从模型身上读出证据，两把尺子都有了还得定下它们在哪一步生效，位置定了阈值还得有个来处 |
| 2.2 | 两类噪声的说明拆短，去掉表现为与包括的连续罗列 |
| 2.3 | 问题设定三处特点改为方法的形状基本是被它们逼出来的 |
| 3.1 到 3.4 | 模块叙述口语化，没有质量标签的时候任何一路证据都有看不见的地方，前三个环节合起来是一个可以安全试错的场子 |
| 4 章首 | 本文的做法压在几个还需要验证的判断上 |
| 6.1 | 九条问题逐条改写，属实现缺陷改为这是实现上的缺陷根子在于什么 |
| 校验 | 320 段 7 表 301 公式，无重复段落，无被压平的含公式段落，28 条文献全部有引用 |

**第九批，语域回调**

第七批与第八批矫枉过正，为了避开仿写把学术书面语一并丢掉了，开头写成了风电场要预测明天的出力这类具体场景，正文里出现了根子、场子、走法、露馅一类口语。参照论文值得学的是书面语之下清晰的句子功能序列，不是口语本身。

第一段的句子功能按参照论文逐句对齐，依次为抽象总起、给出原因、定义核心概念、说明价值、引入规模、规模转为问题、收束到需求，措辞在不打乱序列的前提下自行组织。

| 项 | 改动 |
|---|---|
| 第一段 | 开头改回抽象层面的研究重心转移，随后收缩到时序基础模型，定义部分改为语料在结构完备性与信噪水平两个维度上满足训练需求的程度 |
| 第二段 | 面向这一需求，时序数据治理作为高质量数据准备的重要环节 |
| 局限总起 | 上述方法在各自设定的场景下均能取得良好效果 |
| 挑战段 | 其一在于判定依据的来源本身带有偏差，其二在于试错的代价作用于不可恢复的对象 |
| 第 1 章到第 6 章 | 全部回调至书面语，约束驱动是这一方向最成熟的思路、数据估值提供了另一种视角、裁决之后的控制流有五个去向等表述恢复 |
| 校验 | 根子、场子、走法、顶上去、露馅等口语词全文为零，与参照论文的十字以上共有片段仍只有本文的主要贡献如下一句 |

**2026-08-23，B4 到 B6**

B4，注入候选取最不激进设定。`INJECT_PARAMS` 里 DESPIKE 由空字典改为
`n_sigma=6.0, max_width=1`，RESEGMENT 由空字典改为 `min_keep_frac=0.9`。
空字典落回算子缺省即其常规工作点而非保守端，故 3.4 节所述设计此前只对
去噪与插补成立。实测验证而非断言，在带一处两点尖峰与一处单点尖峰的窗口上，
注入配置改 1 点而缺省改 3 点。方法层哈希由 `6741ded339e5a8df` 变为 `fcd7ba0bf05d5277`。
3.4 节措辞由最保守改为最不激进，因为插补的保守在于不假设周期而其余三个在于幅度。

B5，消融由八档重建为六档并引入 SPO。
`run_ablations.py` 现构造 `SPOPolicy` 并共享一份画像聚类，
使档位之间的差别只来自档位本身而不来自不同的语料划分。
新增 `SPOPolicy.shaped_reward` 开关承载以原始效用为奖励一档，
实测在被结构条件否决且效用变化为 3.0 时，屏蔽奖励给 -0.010 而原始奖励给 +3.000。
该开关只改学习内容不改提交内容，正是 3.3 节第二重职责所需的对照。
`f_single_step` 与 `g_no_abstain` 移除，理由记在 `ladder()` 的文档串里，
两者在 4.2 节没有落点也不隔离方法声称的任何部件。

B6，三处超参数与两处文档。
`calibration.calibrate` 的 K 由 40 改为 50。
`VerifyConfig.eta` 由 0.62 改为 0.5，旧值没有可查的推导过程，
且风险条件在该取值下从未触发，xl 语料上旧式 R(a) 否决 0 次新式 16 次。
新增 `epsilon_from_spread`，按 4.1.5 节所述取效用读数标准差的十分之一，
`VerifyConfig.epsilon` 保留为回退值并在注释中说明优先用换算路径。
文档段落 194 已是五组。段落 176 与 177 合并，保留数字准确的一段。

自查规矩加第四条交叉核对，理由与 19% 那次的发现路径一并记进
`docs/number_selfchecks.md`。

测试 98 passed。

**2026-08-23，B1 变点层改秩式判据，两族均填满**

判据在任何筛选之前单独提交（`22b7a12`），见
`docs/changepoint_criterion_preregistration.md`，其中写清了落选方案与理由。

先说为什么换而不是调阈值。三种单位下测了 400 窗口每族，没有一种能把两族放到同一尺度上。
金融在分块稳健尺度与合并标准差两个单位下的中位数都高于工业的九十分位，
这不是调参问题而是数据性质，利率曲线整体平移是常事而变压器换状态不是。

新判据取窗口内秩。统计量 t 为分段均值之差除以合并的段内标准差，
只读窗口自身取值，不碰注入器的任何内部量。三道门槛为尺度保持、不同时是稀有层、不在注入池。
配额按两族在池中的占比分配，族内按 t 排序取前若干。

筛出结果，1600 窗口池，工业金融各 800。

| 数据集 | 池中 | 稀有有效 | 合法变点 |
|---|---|---|---|
| ETTh1 | 268 | 45 | 37 |
| ETTh2 | 266 | 20 | 22 |
| ETTm1 | 266 | 28 | 31 |
| Crypto | 60 | 3 | 5 |
| Oil Price | 168 | 8 | 20 |
| US Term Structure | 572 | 11 | 65 |
| 合计 | 1600 | 115 | 180 |

| 族 | 池 | 配额 | 合格 | 取用 | 缺口 | t 最小 | t 最大 |
|---|---|---|---|---|---|---|---|
| 金融 | 800 | 90 | 378 | 90 | 0 | 3.608 | 11.126 |
| 工业 | 800 | 90 | 506 | 90 | 0 | 1.392 | 8.829 |

两族均无缺口，六个数据集全部提供该层，对比旧判据下 4 个窗口与四个数据集为零。
被拒原因几乎全在尺度门槛，金融 416 工业 225，同时是稀有层的金融 6 工业 69。

放弃的东西写在预注册里，秩总会返回结果，无法报告某族没有合法变点。
补偿是选中层的 t 分布与注入 level_shift 的 t 分布并列报告，
若两者相差很远则该层是比它自称更弱的检验，无论结果如何都报。

**2026-08-23，LTSV 进入基线，4.1.3 节补三段实测**

基线由七项增为八项。LTSV 作为第 7 项进入，同时写进引言表 1 的打分族一格、
相关工作 1.2 节与参考文献 [29]，标注 DASFAA 2026。
选它的理由写在正文里，它与本文取证环节最接近，两者都读冻结模型对一个窗口的反应，
差别在于它只据此给分，本文据此选择并裁决一次编辑。

4.1.3 节新增三段，全部是实测而非说明。

第一段是 TSRating 不进基线清单的依据。翻转率 0.508，
首位得票率 73.6% 中位 76.0%，60 个配对维度中 17 个不低于 90%，
两序平均后 24 个落在方法自身的未决区间内。
结论限定为关于后端替换的结论而非关于该方法的结论。

第二段是成本外推，两千窗口乘四维约 16000 个配对维度、约 2100 元、32 小时，
作为主表保留计算开销一列的实测依据之一。

第三段把 19% 这个比例提到正文使用。210 窗口中 40 个因注入缺失而无法切块，
这是打分族在缺失数据上结构性局限的实测量，与本文具备插补算子形成直接对照，
该数据支撑主表中打分族与本文方法在缺失类缺陷上的差异。
缺失窗口规则一并写入，即不丢弃而作为未选中带进各打分臂，
事前插补被否决因其会把本文算子产物送进基线输入。

1.2 节结尾补上同一处限制，使基线在第 4 章的处理与第 1 章的调研对得上。

校验：29 条文献全部有正文引用，277 段 5 表 304 公式，破折号分号引号为零。

**2026-08-23，B3 打分族四项跑通，heavy 语料**

`valuation_export.py` 在主环境导出，`valuation_score.py` 在 dataval 环境算分，
两者以文件交换，环境断言通过，`opendataval` 1.3.0、`numpy` 1.25.2、`sklearn` 1.9.0。

语料 210 窗口，可切块 170，**不可切块 40**。后者正是注入了缺失的那批，
按 `docs/valuation_family_protocol.md` 新增的规则作为未选中带进各打分臂而不是丢弃。

| 方法 | 打分窗口 | 秒 | 均值 | 标准差 |
|---|---|---|---|---|
| TimeInf | 116 | 5 | 0.000000 | 0.029841 |
| Data-OOB | 116 | 82 | -16.763183 | 51.049137 |
| Data Shapley | 116 | 46 | -0.079459 | 1.307168 |
| KNN Shapley | 116 | 0 | 0.000004 | 0.000016 |

两处实现修正记下。一是 `opendataval` 1.3.0 的 `DataEvaluator.train` 收 `DataFetcher`
而不是原始数组，改用 `from_data_splits` 交接三个划分，仍是 TSRating 基线文件构造的同一对象。
二是 `third_party/TimeInf` 未同步到节点，补传后 TimeInf 通过。

计算开销一列的实测依据由此产生，四项合计 133 秒于 464 个训练块，
与本文只做前向的探测形成 4.1.4 节所述的跨族对照。

**2026-08-22，A1 到 A3 实验设置收口，同步文档 4 章首、4.1.4 与 4.2 节**

纯文档改动，不依赖任何外部条件。

A1，实验由六组收为五组。

| 新 | 内容 | 来源 |
|---|---|---|
| 主实验 | 统一语料层协议下与七项基线同表，报五个主表指标 | 新增，原先无独立主实验组 |
| 实验一 | 验收信号的失效刻画，含全部基线 | 原实验一 |
| 实验二 | 探索的结构安全性，验证定理 4，含全部基线作参照行 | 原实验三 |
| 实验三 | 策略学习的收益，含全部基线 | 原实验四 |
| 消融与敏感性 | 只含本文方法各档变体 | 原实验六 |

原实验二奖励塑形消融并入消融档位，成为以原始效用为奖励一档，
理由是它本属消融性质且原文自述不含基线。
原实验五预算效率并入主实验，收益侧的节省探测次数与代价侧的漏检窗口数作为主表两列，
两列必须同时出现，只报节省不报漏检会让该机制显得没有代价。

A2，消融由八档收为六档，即无屏蔽、无结构条件、无策略学习、无候选注入、
以原始效用为奖励与完整方法。三档去除的依据写进正文而不只写在提交信息里。
无效用条件去掉，因为实测效用条件否决 769 次而结构条件 616 次，
去掉它之后的接受集与无屏蔽相差很小，信息重复。
无预算分配去掉，因为效果已由主实验的开销列与探测次数两列呈现。
无同类校准去掉，因为早期实测已证明它与全局标准化在判别力上无可检出差别，结论保留在 6.1 节。
三档均基于已有实测而非预算考虑，若后续数据推翻任一条则对应档位恢复。

A3，主表指标收为五个，即下游预测误差、受保护层误编辑数、损害率按定义 1、
修复增益、计算开销。决策质量四类降为分析层，进正文讨论不进主表，
理由是它们只在产出逐动作裁决的方法上有定义，打分族一律不适用，
放进主表会得到一张多数格子为空的表。计算开销进主表而非附注，
因为各族单位代价相差很大，不把开销与效果并列则跨族比较只剩半张表。

交叉引用同步，3.5 节原先指向 4.2 节实验六的一处改为指向消融与敏感性一组。

**2026-08-22，裁决一 两层改筛选式，判据先预注册**

`docs/stratum_selection_preregistration.md` 在任何筛选跑之前单独提交（`287babb`），
判据与全部阈值写死在其中。本批实现判据并如实报出筛出数量，一个阈值都没有动。

| 项 | 改动 |
|---|---|
| `corpus.py` | 新增 `channel_reference`、`is_rare_valid`、`best_split`、`is_changepoint`，全部按预注册的六个常数 |
| `datasets.py` | 新增 `channel_columns`，筛选判据要对窗口所属通道的完整历史表达，所以需要取列 |
| `stratum_selection.py` | 新脚本，按预注册跑出筛出数量、逐数据集分布、被拒原因拆解，以及预注册承诺的跳变分布三单位对照 |

筛出结果，1600 个窗口的池子，工业金融各 800。

| 数据集 | 池中 | 稀有有效 | 合法变点 |
|---|---|---|---|
| ETTh1 | 268 | 45 | 2 |
| ETTh2 | 266 | 20 | 0 |
| ETTm1 | 266 | 28 | 2 |
| Crypto | 60 | 3 | 0 |
| Oil Price | 168 | 8 | 0 |
| US Term Structure | 572 | 11 | 0 |
| 合计 | 1600 | 115 | 4 |

稀有有效层可用，命中率 7.2%，六个数据集都能提供。

**合法变点层不可用，按预注册如实报告而不是放宽判据。** 4 个窗口，六个数据集中四个为零。
被拒原因几乎全部是 B1 跳变不够，工业 792/800 金融 799/800，B2 与 B3 合计不到 5 个。

原因是我在预注册里写错了单位。预注册称阈值 2.5 取自注入范围的下沿，
而注入按窗口尺度施加 2.5 至 4.0 倍，判据按通道 IQR 表达，两者相差三分之一到一半。
预注册自己要求的跳变分布对照把这一点测了出来，
以通道 IQR 计，注入后跳变的中位在工业为 1.908 金融为 0.991，阈值 2.5 落在其上四分位而不是下沿。

三种单位的对照一并记下，均由 `experiments/stratum_selection.py` 跑出。

| 单位 | 工业真实 q99 | 工业注入 q05 | 金融真实 q50 | 金融注入 q05 |
|---|---|---|---|---|
| 通道 IQR | 2.468 | 0.520 | 0.325 | 0.311 |
| 窗口尺度 | 1.633 | 0.920 | 1.031 | 0.914 |
| 分块稳健尺度 | 4.464 | 2.210 | 3.257 | 3.788 |

窗口尺度这一单位对选择器无用，注入之后窗口自身尺度被跳变撑大，
注入的跳变一律落在 1.0 附近，与真实窗口不可分。
分块稳健尺度对跳变免疫，在工业上 2.5 恰好落在注入分布的 q05 且约 6% 真实窗口达标，
但在金融上 65.7% 的真实窗口已经超过 2.5，因为价格的长期漂移远大于其日间波动。
两种单位都无法同时适配两族，故该层暂标 deferred，等口径裁决。

`build_corpus` 仍在用旧的构造式两层，未切换，切换与金融语料接入合并做。

**2026-08-22，第一项 R(a) 前后对照补齐三个口径**

同步文档 2.3 节。运行经 monitor 起动，6 项前置断言通过，
代码哈希 `6741ded339e5a8df`，配置哈希 `c4f1e303885a6c5d`，
xl 两千窗口，seed 42，tau 0.02，三后端池。

改动清单。

| 项 | 改动 |
|---|---|
| 规模 | 从 800 窗口的自造配比改为 `SCALES["xl"]`，即学习循环与预注册使用的同一份语料 |
| 风险否决单列 | `score` 统计每个 `Verdict` 的出现次数，`risk_vetoes` 与效用、结构两项分列 |
| 选择性执行 | 新增 `selective_error`，按风险状态置信度排序被编辑窗口，报最确信十分之一的误编辑率、全体误编辑率与两者之差，并给出五档覆盖率曲线，口径与 `results/confidence_and_ood.md` 一致 |
| 第三臂 | 原脚本把方差项、分布外判据与风险替换三处一起翻转，只能回答这一批是否有用。新增 `depth_only` 臂只翻转风险替换，用于隔离本项 |
| 分层 | `PROTECTED` 按 4.1.2 裁决收到四层，`clean_ood` 移出并单列为 `probe_layer_damage` |
| 前置断言 | 接入 monitor，并加一条入口断言，`USE_DEPTH` 若不为真则拒绝运行，因为那意味着部署的缺省与 2.3 节不符 |
| `--n-jobs` | 节点报的是宿主机 208 核，缺省会去 fork 206 个工作进程并失败。只影响画像提取如何切分，不影响算出什么 |

USE_DEPTH 核实结果。缺省为 `True`，与文档一致。
用 AST 扫描全仓，赋值点只有两处，`agent.py:53` 的定义与 `fix_compare.py` 的对照开关，
确认后者是唯一置假处。

**2026-08-22，第三项 定理 6 的实证接口**

只加记录不改决策，同步文档 3.5 节定理 6 说明段。测试 95 加 3 为 98 passed。

| 项 | 改动 |
|---|---|
| `spo.py` `SPOConfig.t_cal` | 新增，缺省 500，即文档 4.1.5 给的每五百次决策 |
| `spo.py` `SPOPolicy._record_gap` | 在 `order` 里记下每次两个以上可行动作时最优与次优置信上界之差 |
| `spo.py` `SPOPolicy._close_interval` | 按标定间隔分段，记该期间被更新格子的最小访问计数。最小值只在被更新的格子上取，未被触及的格子价值不变，把它的零访问计入会让上界发散于一个与被界量无关的理由 |
| `spo.py` `SPOPolicy.theorem6_report` | 输出 gamma 的经验分位、每个区间的 n_min、以及各分位下 2 q T_cal 除以 gamma n_min 的上界。gamma 为零时报无穷而不截断，因为那正是定理本身的内容 |
| `monitor.py` `LEARN_CONFIG_KEYS` | 加入 `t_cal`。预注册的漂移测试立即拦住了这次改动，随后补进 `docs/spo_preregistration.md` 的配置表 |
| 配置哈希 | `669ce2f1341ed6ca` 因加入 `t_cal` 变为 `3b38ecf2a2f5a51f`，两个旧值都在预注册里标为被取代 |
| `spo_learn.py` | 新增 `--t-cal`，纳入 `cfg_dict`，结果新增 `theorem6` 字段 |
| 测试 | 新增三条。一条锁住记录不改决策，用同种子驱动开关记录与不开记录两条策略并断言动作序列逐步相同。一条断言报告带齐上界的三个量。一条断言 gamma 为零时报无穷 |

一处先行观察。未访问格子共享同一乐观初值与同一访问计数，其置信上界完全相等，
只要某个状态上有两个以上这样的格子同时可行，gamma 就恰好为零而不是接近零。
最坏情况形式因而在探索早期是按构造失效的，不是数据碰巧。合成自检里该比例为 0.15。
实测比例待实验六给出，已写入文档 3.5 节。

**2026-08-22，文档与代码差异核对**

本轮只读不改代码。完整读 0821 文档的 2.3、2.4、3.1 至 3.5、4.1、4.2 各节，
逐条与工作区代码比对，产出 `docs/doc_code_diff.md`。测试基线 95 passed。

| 项 | 结论 |
|---|---|
| 决策风险 R(a) | 已符合，`verify.py:76` 三项加权式与文档式 77 完全一致，后验版只留在对照开关里，缺的是对照报告的三个口径 |
| 金融数据集 | 缺口，`datasets.py` 只有 ETT 与 exchange 与 solar，`data/` 下无金融文件 |
| 强制候选注入 | 部分符合，机制与哈希都到位，最保守参数只对去噪成立，去尖峰与重分段落回算子默认 |
| 定理支撑 | 定理 2 有 `soft_vs_hard.py`，定理 6 的 n_min 与 gamma 与 T_cal 三个记录量全缺 |
| 实验档位 | 消融八档只对上五档，缺的三档需先把 SPO 接进消融路径 |
| 分层口径 | 文档四层代码七层，且合成 OOD 层与文档不用合成构造这句话冲突，需裁决 |
| 系统噪声 | 缺口，`inject()` 作用于一维序列，没有多通道联合注入 |
| 超参数 | K 与 eta 两处不一致，eps 未按文档的估计方式实现，T_cal 代码里没有 |
| 文档自身 | 段落 194 的九组与 4.2 的六组对不上，段落 176 与 177 内容重复 |

绘图提示词按 PROCore 图 1 与图 5 的风格重写后直接给出，未写入文档。

**第十批，命题并入定理并补齐证明**

| 项 | 改动 |
|---|---|
| 编号 | 取消命题这一提法，原命题 1 软惩罚的损害下界并入定理体系成为定理 2，原定理 2 至 6 顺延为定理 3 至 7。用占位符两步替换以免中途覆盖，全文 24 段涉及 |
| 计数表述 | 摘要与结论中六条定理与一条命题改为七条定理，枚举中补入软惩罚的损害下界 |
| 定理 6 证明 | 原为证明的思路分三步，改为完整推导。第一个不等式由总变差距离的变分定义配合零一风险函数的有界性给出，再与定理 5 的校准保证相加。第二个不等式分两步，先由增量更新式与奖励截断界定单次改变量不超过 2q 除以最小访问计数，再由三角不等式得累积改变量，然后利用确定性策略下排序翻转需要价值改变量达到间隔一半这一事实，把价值变化换算为总变差距离 |
| 核对 | 一条引理与七条定理均紧跟证明段并以证毕收尾，全文无见附录、略去、从略、限于篇幅一类回避性表述 |

### 代码

无代码改动。新增四个文档改写脚本 tools/rw20_algos.py、rw20_tables.py、rw20_eq6.py、rw20_ch4.py。

### 提交

- `7970492` 产出 0820 版本，算法框公式化，删除属性表

---

## 2026-08-19

### 文档 胡宏彬-进度文档-20260819.docx

在 0818 基础上整篇重写，0818 备份为 -backup。

| 节次 | 改动 |
|---|---|
| 全文 | 公式从纯文本转 Word 公式对象，0 到 192 个 |
| 全文 | 27 条参考文献在正文全部标注，此前 PatchTST DLinear 变点检测三条悬空 |
| 2.1 | 删除 2.1.1 到 2.1.4 四个小节标题，词条式改为连续叙述 |
| 3.4 | 补三表结构、暖启动方式、尾簇合并、奖励上界与定理 5 的关系 |
| 3.3 | 定理 3 对任意候选来源成立 |
| 3.4 | 乐观初始化的作用边界与注入机制、策略学习的两类增益、预算跳过的相对判据 |
| 4.1.3 | 补两套评测协议与全基线清单 |
| 4.1.6 | 新增，k 的选取判据、AMI 混淆检查、语料划分 |
| 4.2 | 八组实验去掉预期结果，实验五改为收益与代价同表 |
| 6.1 | 三条阶段零发现，九条并列清单归为三类 |
| 6.3 | 监控工具的代码哈希机制 |
| 全文 | 六处空壳强调句清零，不是 A 而是 B 从 24 处降到 1 处，破折号分号引号清零 |

### 代码

| 文件 | 改动 |
|---|---|
| `src/introact_ts/spo.py` | 新增 SPOPolicy，UCB 选择与增量更新，三张表按撤销原因路由 |
| `src/introact_ts/spo.py` | 新增 inject 与 INJECT_PARAMS，未访问格按概率强制入候选 |
| `src/introact_ts/agent.py` | curate_window 加 policy cluster budget 三个可选参数，默认行为不变 |
| `experiments/spo_learn.py` | 新增，学习循环加预算分配 |
| `tools/monitor.py` | 配置哈希断言改为强制，新增 code_hash 与 LEARN_CONFIG_KEYS |
| `tools/docx_rewrite.py` | 新增，markdown 转 docx 段落含公式对象 |
| `tests/test_spo.py` | 15 项 |
| `tests/test_monitor.py` | 13 项，含配置键与预注册文档的一致性检查 |

### 实验

| 结果 | 内容 |
|---|---|
| `results/spo_learn.json` | xl 学习循环，261 决策，盲区两格访问 0 次 |
| `results/spo_learn_p02/p05/p10.json` | 注入概率三档，注入触发 2 到 4 次，盲区仍无法判读 |
| `results/mtcsc_overlap.json` | MTCSC 与 SCREEN 三层重合，窗口级 1.0000 值级 0.0038 |
| `results/spo_clip_sensitivity.json` | 截断分位三档，95 与 99 同动作比例 0.9860 |

### 提交

- `240a1fb` 注入概率扫描，文档 3.3 3.4 4.2 修正
- `7647a5a` 提议层强制注入，配置断言强制化
- `cef52dc` xl 学习循环，盲区未被提议
- `e5eede2` UCB 与预算分配接入
- `250bd81` 线 C 四处文档落地，学习循环预注册
- `e9af637` 0819 版本重写

## 2026-08-26 三 seed 主表与交叉核对

### 实验

三 seed 主表全部跑完，xl mixed，code hash `1f6d57bb1a15e4a0`，配置哈希 seed0
`573d525c8c8187c2` seed1 `989f53e2c3ab5525` seed2 `1d85ed2d6b503ae7`。窗口数
1986 2000 2000。

| 臂 | edits | 误编辑率 | 损害率 | nRMSD |
|---|---|---|---|---|
| L0 no_action | 0 | 0.0000 | 0.0000 | 1.1531 +- 0.0607 |
| L1 SCREEN | 355 +- 25 | 0.1779 +- 0.0168 | 0.6882 +- 0.0342 | 1.1505 +- 0.0531 |
| L2 IMR | 1941 +- 12 | 0.9885 +- 0.0025 | 0.8646 +- 0.0011 | 1.1045 +- 0.0491 |
| L3 MTCSC | 355 +- 25 | 0.1779 +- 0.0168 | 0.7213 +- 0.0225 | 1.0983 +- 0.0510 |
| L7 Learn2Clean | 361 +- 10 | 0.1760 +- 0.0007 | 0.6887 +- 0.0287 | 1.1082 +- 0.0608 |
| utility_only | 556 +- 19 | 0.1716 +- 0.0124 | 0.4203 +- 0.0210 | 1.0590 +- 0.0463 |
| spec_veto | 386 +- 10 | 0.1340 +- 0.0058 | 0.1597 +- 0.0114 | 1.0302 +- 0.0505 |
| introact | 161 +- 9 | 0.0635 +- 0.0054 | 0.1287 +- 0.0107 | 1.0754 +- 0.0458 |
| oracle | 737 +- 2 | 0.0000 | 0.0000 | 0.0000 |

introact 损害率 std 0.0107，预设阈值 0.05，未超。

### 交叉核对查出两处

一 轨迹里的 `dist_before` `dist_after` `nrmsd_after` 用 `_dist`，主表损害率用
`_nmse`，两者对非有限点一个丢弃一个填中位数。缺失类污染的窗口在 `_dist` 下距
离恒为 0，于是任何填补都被判为损害，spec_veto 的损害率被读成 0.4627 而不是
0.1465。在 L1_screen 上离线精确复现主表的 329 edits 与 0.6657，确认主表对，轨
迹字段错。这三个字段不能用于重算损害率。

二 nRMSD 列被 level_shift 子群主导，105 个窗口贡献均值的 62%。两个缺失类在这
个口径下对不作为的臂计 0 分，占分母的 28.6%。introact 与 spec_veto 在该列上的
排序随子集翻转，全集配对中位数 introact 更优 p 1.9e-13，剔除缺失类后 spec_veto
更优 p 0.0193，均值方向又与中位数相反。该列的排序在口径定下来之前不进正文。

### 检验

误编辑上 introact 编辑的保护窗口是 spec_veto 的严格子集，三 seed 的不一致对分
别为 0 比 72 0 比 80 0 比 69，精确 McNemar p 4.2e-22 1.7e-24 3.4e-21。

### 代码

| 文件 | 内容 |
|---|---|
| `experiments/aggregate_seeds.py` | 新增，多 seed 汇总 mean 与 std，含臂集与列集一致性断言 |
| `experiments/reconcile_traces.py` | 新增，用落盘轨迹独立重算主表三列并对账 |
| `experiments/damage_metric_probe.py` | 新增，在无 GPU 的确定性臂上定位两个口径的差异来源 |
| `experiments/arm_significance.py` | 新增，配对 McNemar 与 Wilcoxon，不依赖 scipy |
| `docs/number_selfchecks.md` | 三 seed 主表的四条自检，两处发现 |

### nRMSD 裁决，分污染类型报，不重跑

裁决 2026-08-26。列保留，聚合方式改。单一均值被 level_shift 一个子群主导，
这是聚合的问题不是指标的问题。`experiments/nrmsd_by_contamination.py` 从已落盘
的三 seed 轨迹提取，零 GPU。

正文主表的 nRMSD 改为五个可信类型的按窗口数加权均值，表注写明排除两个缺失类
及理由。分类型完整数字进附表。

| 臂 | 可信类型加权均值 |
|---|---|
| L0 no_action | 1.6161 +- 0.0850 |
| L1 SCREEN | 1.5853 +- 0.0666 |
| L2 IMR | 1.4114 +- 0.0699 |
| L3 MTCSC | 1.5306 +- 0.0713 |
| L7 Learn2Clean | 1.5463 +- 0.0868 |
| utility_only | 1.4435 +- 0.0658 |
| spec_veto | 1.4359 +- 0.0717 |
| introact | 1.5067 +- 0.0645 |

这一列最低的是 IMR，而 IMR 的误编辑率 0.9885 损害率 0.8646。它靠重写几乎每一
个点把 spike 压到 0.2992，代价是别的全毁。该列不得脱离误编辑与损害两列单独读，
正文必须点破。

分类型后 introact 与 spec_veto 的对照出现真实分歧，level_shift 与 duplicate
上 introact 更优，spike 上 introact 明显更差，中位数差 +0.3715 p 0.00093。汇总
的 +0.0067 是这两个方向抵消后的残差。spike 那处是讨论必须解释的，尖峰正是结构
条件最可能读成真实特征而拒绝移除的形态。

实际语料的污染类型是七类 duplicate flatline level_shift missing_block
missing_scattered noise spike，与规划里写的 point_outlier contextual_anomaly
noise_injection 三个名字不对应，按语料实际标签分组。

### 选择臂的误编辑率改为显式 0

L4 L5 L6 此前不写这个键，汇总渲染成 n/a，读起来像没测。改为显式 0 并带
`mis_edit_rate_note` 说明是构造上为零而不是谨慎所致。`aggregate_seeds.py` 对旧
结果文件补同一个默认值，所以三 seed 不需要重跑。

### 消融脚本改造，使其能与主表并列

`run_ablations.py` 原本跑出来的行无法与主表并列，三处不一致，都是读代码查出来的。

一 语料。原脚本硬编码 `build_corpus(spec, source="ett")`，主表是 mixed。改为
`--source` 参数，默认 mixed。

二 指标。原脚本用 `metrics.summarise`，它根本不产出 protected_mis_edit_rate
与 repair_nrmsd 这两列。改为直接调用 `run_main.score_rows`，四列同名同义。

三 轨迹。改为与主表同一个 `dump_window_traces`，落 JSONL，所以消融的口径变更
也能离线重算。

另加多 seed 循环、逐级逐 seed 的结果文件跳过、以及补上缺失的 A4。

### 两处此前未记录的事实，都影响正文表述

一 **主表的 introact 行是固定规则，不含策略学习。** `run_main.agent_traces`
调用 `curate_window(w, s, peer_idx=i)`，没有传 policy 与 cluster，而
`agent.curate_window` 的文档写明无 policy 时用提议器自身顺序，即固定规则。所以
主表的 introact 等价于消融的 `c_no_policy`，完整方法是 `f_full`。正文凡是同时
出现主表与消融的地方都要写明这个区别，冒烟的验收标准也据此改为 `c_no_policy`
对照主表 introact 四列一致，而不是 `f_full`。

二 **共形标定是离线接入的。** `src/introact_ts/conformal.py` 不被 run_main 或
run_ablations 调用。流程是 `run_conformal.py` 先产出 `results/conformal.json`，
其 alpha 0.03 处的 lambda_star 为 0.02，主表再以 `--tau 0.02` 启动。因此 A4 无
共形标定这一级实现为把标定前的手工值 0.12 放回去，该值记录在 conformal.py 的文
档字符串里，是本项目在程序存在之前实际用过的数，不是现在为了输而挑的。

### 冒烟只能在服务器做

本地无法加载模型池，`make_pool` 报三个后端都缺依赖，chronos-forecasting 与
timesfm 都没装。所以消融的冒烟排到服务器上机第一步，在 small mixed seed 0 上比
`c_no_policy` 与主表 introact 的四列。

### 消融 seed 0 七级，策略三级与参考行逐位相同

屏蔽三级退化清晰，A1 无屏蔽损害率 0.3958，A2 无结构条件 0.3508，A4 手工阈值
0.2172，参考行 0.1402。策略三级 A5 无学习 A6 无注入 A3 原始奖励，四列与参考行
一模一样。

排除接线故障用了三条独立证据，不是只看调用点。agent.py 201 行调 policy.order
263 行调 policy.observe，闭环存在。结果文件里 n_decisions_with_choice 为 423，
说明 UCB 真的在多个可行臂之间做过选择。逐窗口轨迹显示 265 个窗口的动作顺序与固
定规则不同，37 个窗口步数不同。策略在跑也在改变尝试过程。

它没改变的是终点。1986 个窗口逐个比对最终状态，相同 1986 个，不同 0 个。

注入是唯一能改变接受集的机制，重排只能置换固定的候选表。它触发了 20 次，多出
来的 25 个动作里 17 个 NO_OP 算子根本不适用，8 个被屏蔽回滚，被接受的 0 个。

注入稀疏的原因在 SPOPolicy.inject，只对该表该簇未访问的算子注入，还要排除本窗
口已试过和候选池已有的，最后才以 0.05 概率触发。未访问集合随学习很快清空。而
提议器从不给出的算子多半是不适用的，17 个 NO_OP 正是这个。

结论。这不支持策略提升治理质量这句话，四列增益为零，效率增益 16 次探测 16 步
即 0.47%。它是定理 4 探索结构安全性的直接证据，探索重排了 265 个窗口又注入了
20 个提议器从不给的算子，提交集一个窗口都没动。梯子本身要证的结论不受影响，屏
蔽承担了全部结果。

方向待裁决，三条路已提交给决策，倾向把 SPO 的主张从增益改为安全性。

### 屏蔽保守性重算，ett 上的旧数字作废

`shield_replay.py` 加了读 JSONL 的分支，默认语料从 ett 改为 mixed，默认臂名从
introact_full 改为 introact。原来的 0.187 与按算子的那组数是在 ett seed 42 上
算的，而 4.2 节的表在 mixed 上，不能混用。

重算后注入层从 0.187 变成 0.2855 加减 0.0106，按算子的排序整个翻转，RESEGMENT
从最差 0.278 变成第二好 0.1820，DESPIKE 从完美 0.000 变成第二差 0.3711。这是第
三次出现 ett 上的数字搬不到 mixed，前两次是修复距离和选择基准。

核心结论稳健且更强。四个受保护层加探针层，三个 seed 全部为 0.0000，被屏蔽拒绝
的候选里没有一个会改善它本该保护的窗口，保守性的代价全部落在注入层。受保护层
上被拒候选的距离中位数在 +0.15 到 +0.54 之间，说明那些拒绝不是边缘判断。

重放覆盖率这次几乎是满的，每个 seed 只有 2 到 3 个被先前编辑取代，失败 0 个，
而 ett 那次是 449 个取代 39 个失败。模块文档里警告的那个局限在本语料上不成立。

两个平滑算子 DENOISE 0.4197 与 DESPIKE 0.3711 是屏蔽最常判错的地方，它们恰好也
是效果最难与移除真实结构区分的，而结构条件正是为防这个而设。IMPUTE 承担了 78%
的拒绝且最可靠，0.1280 加减 0.0014。这一条正好解释了 introact 在 spike 上修得
不如 spec_veto，尖峰正是 DESPIKE 与 DENOISE 看起来像在削结构的形态。

## 2026-08-27 软惩罚接入主表

### 方法层，`VerifyConfig.soft_mu`

主表的 soft_penalty 行此前是 deferred，理由写的是 VerifyConfig 表达不了加权和
判据。现在给它加一个可选字段 soft_mu，非空时把合取换成 dU 减 mu 乘 D_struct 大
于 epsilon，且对畸变本身不设阈值。默认 None，现有调用一律不受影响，两条测试分
别钉住这两半。

拒绝在软规则下报为 ROLLED_BACK_UTILITY，因为软规则只有一个分数可失败，没有独立
的结构否决可报，这正是对照要说明的事。

这样软臂就与其余对照走同一条 agent_traces，语料提议沙箱探测评分全部相同，唯一
差异是判据，四列由 score_rows 直接产出，能与我们的行并列。

方法层代码哈希由 `1f6d57bb1a15e4a0` 变为 **`4dafbe9f0e36e7e4`**，后续所有跑要用
新值断言。

### `experiments/run_soft_sweep.py`

新增，mu 十档乘三 seed，走 run_main 的臂调度与指标。按要求实现熔断恢复，每个
(mu, seed) 写临时文件再 rename，rename 是原子的，所以断电只会留下临时文件不会留
下截断的结果。已存在且能解析的组合跳过，存在但解析失败的删掉重算。感知每个 seed
算一次，某个 seed 的全部档位都在盘上时连感知一起跳过。

`soft_vs_hard.py` 保留不动，它的前沿分析回答的是另一个问题。原来打算从它取数填
主表那一格，但它用的是 ett 语料、自建的 CorpusSpec、把 clean_ood 计入受保护层、
以受保护层平均 nmse 当损害率、提议序列来自 stat_only 而非 agent，这样产出的行填
不进主表。

### 软惩罚扫描完成，定理 2 的实证支持不足以支撑强断言

十档权重乘三 seed 全部跑完，交叉核对 30 对 30 全对，mu 0 那档在三个 seed 上都
与主表的 utility_only 行逐位相同，接线校验通过。

结论与预期不同，如实记录。软臂在每一个权重下修复都比我们好，可信类 nRMSD 在
1.4425 到 1.4932 之间而我们是 1.5080，代价是更多编辑和更多损害。两种设计是同一
条权衡曲线上的两个点，不是一方全面占优。

没有任何权重达到我们的损害率，最低是 mu 100 的 0.1531 对我们的 0.1306，三个
seed 方向一致。但这个差异不显著，三 seed 符号检验 p 0.250，合并窗口的两比例检
验 88 比 576 对 63 比 482，z 1.022 双侧 p 0.307，区间也重叠。所以不能写软惩罚够
不到合取判据的损害水平，那句话本数据支持不了。

能站住的是形状。修复列在 mu 0.5 见底 1.4425 之后单调劣化到 1.4932，而损害率到
扫描末端仍在下降且仍高于我们。也就是过了 mu 0.5 这个设计开始拿修复换损害下降，
跨四个数量级仍未买到合取判据的损害水平。定理 2 的实证表述改为这个形状陈述，严
格下界保持为理论命题不声称已测得。

要把 0.022 的差距做显著大约需要十个以上 seed 约八小时 GPU，写下来让这个取舍可见
而不是用沉默带过。


2026-09-14 18:49 最新冻结：40 passed in 1.01s，日志 `logs/v43/contracts/20260914T104900.858201Z/`。显存账本补记模型加载峰值，最终峰值取加载与推断最大值。旧队列因代码hash变化正确停止，已确认旧PID退出后重新启动；当前PID 58448，状态 `waiting_for_models`。完整机器可读证据见 `docs/v43_cpu_stage_evidence_20260914.json`，实时状态见 `results/v43/pilot_queue_status.json`。模型/方法结果仍未产生。


## 2026-09-14 持续监控

用户要求持续监控后，启动独立 tmux `work2-v43-monitor-20260914`（socket `/tmp/tmux-1000/default`），监控PID 59254。每30秒只读采集原安装、模型接续和pilot进程树的I/O、打开的wheel/incomplete文件大小、GPU、可用内存、磁盘和错误；不采集命令行，不安装、重启或终止任何进程。连续15分钟安装phase/I/O不变时记录告警。监控持续至pilot完成或24小时窗口结束。

最新快照 `results/v43/monitor_status.json`，完整采样 `/home/vipuser/work/work2/logs/v43/20260914T105516.706494Z-monitor/samples.jsonl`，监控入口 `scripts/launch_v43_monitor.sh`。`python3 scripts/bootstrap_status.py` 已纳入pilot与监控状态；PID不可见时提示先核对主机命名空间。报告写盘不等同于自动发送聊天通知。

18:55确认依赖下载仍推进：nvshmem 124.7MB已完成，开始nvjitlink 19.7MB；真实pilot仍未开始，模型manifest尚缺。


## 2026-09-14 维护原Git仓库

登记origin为ForestBagpipes/TS_Dataflow，保持当前快照内容接入远端master历史；新增每阶段推送及远端SHA核对规则。凭据/数据/权重文件检查未发现待上传项。首次push实际失败于HTTPS身份认证，记录在docs/git_sync.md；没有修改远端master或force push。


## 2026-09-14 下载链路实测

同版8 MiB样本：NVIDIA官方cuDNN直连2.944 / 代理0.191 MiB/s，清华同包直连2.916 / 代理0.256 MiB/s，交大Torch直连0.733 / 代理0.326 MiB/s。新增只读探测脚本及后续下载进程专用的按域名直连配置；原安装仍用原线路，未重启或并发改写环境。完整限制、403探测失败与原始证据见 docs/download_routes_20260914.md。真实pilot仍未运行；本地提交待GitHub认证恢复后补推。


## 2026-09-14 20:00 用户授权下载切线

work2已实际切到任务专用直连分段下载，保留约932MB已有数据。旧安装PID45580/61386退出，新bootstrap PID66506、下载PID66519；模型接续和pilot队列已恢复，监控PID59254保持。代理服务与Codex配置未改，端口切换前后可达。首次切线父进程已退出异常与分段重试全部留痕，详见 docs/download_switch_20260914.md 及机器证据；不能重启旧安装或同时改写prefix。真实pilot仍待验收。


## 2026-09-14 20:15 环境验收完成

三个独立环境全部完成安装、依赖检查和指定模块导入；TS-ICL与Chronos的CUDA256×256矩阵检查通过，RTX4090支持BF16，单次小测试峰值分配9,502,720字节。TS-ICL为Python3.12/NumPy2.5.3，core与Chronos为Python3.11/NumPy1.26.4，两GPU环境保持Torch2.9.1+cu126。25个恢复wheel整包官方哈希校验通过，环境freeze/conda-explicit与SHA256SUMS已落盘并逐项复核。

安装20:14:48结束，模型接续20:15:27开始；TS-ICL官方revision已锁为19c94031439fb31f36ce395088ee50a6762d3774，权重下载中。Bolt尚未下载，真实32-origin pilot仍未运行。此处是环境验收，不是模型worker或方法成功。证据见 docs/environment_acceptance_20260914.json；冻结记录见 requirements/bootstrap-20260914/。待两个模型接口验收通过后，队列重验CPU gate并运行真实pilot，calibration/test读取仍关闭。


## 2026-09-14 20:53 首轮真实pilot失败与入口修复

首个真实pilot于20:49启动，run results/v43/20260914T124856.762289Z-pilot；CPU gate40项通过后，TS-ICL worker在模块导入时因缺statsmodels失败，尚未执行预测或读取future标签。原因是introact_ts顶层入口提前加载旧Agent/profile依赖，而TS-ICL环境按官方依赖保持隔离。没有向TS-ICL环境补装core栈。

已将旧重依赖导出改为按需加载，保留原公共API，并将顶层__init__/types/verify三项必要入口依赖纳入worker代码hash。43项CPU测试通过（1.29秒），4项旧API判据回归通过，两个worker在各自冻结解释器的--help入口均通过。完整失败日志、测试和代码hash见 docs/worker_import_fix_20260914.json。新真实pilot仍须实际执行后验收，不把此修复记为方法成功。

Bolt镜像于20:46:09整包官方SHA256通过，20:46:52在旧传输进程退出和HF文件锁保护下接入同一官方revision缓存；保存旧335,236,791字节未完成文件。原验收器确认Bolt GPU接口通过，TS-ICL也通过。可选Chronos-2元数据TLS失败及一次独立重试失败保留，不阻塞只要求TS-ICL/Bolt的pilot。权重完整镜像耗时与HF缓存命中耗时分别保存，不能混算。


## 2026-09-14 21:00 post-hoc：真实 P1 pilot 完成

32 origins（20 train / 12 dev）、L512/H32、64 次真实 TS-ICL 插补和 96 次 Bolt 预测全部完成；43 项 CPU gate 通过，独立原始结果重算通过，未读 calibration/test。运行 21.794 秒，最大 GPU 分配 710,672,896 字节。KEEP / SINGLE / COV 来源宏平均 MASE 为 1.322762 / 1.316489 / 1.228219；COV 宏平均 MAE 反而变差，两插补臂各 15/32 个 origin task harm，无 CI。仅为接口与开发诊断，正式 A0–A5/H96/H192 未运行，PICS_joint_relabel 不变。首轮导入失败和可选 Chronos-2 TLS 失败保留。证据与原始结果入口见 `docs/v43_pilot_report_20260914.md`、`docs/v43_pilot_evidence_20260914.json`；下一步补长来源、接正式强对照及 A5 静态规则。


## 2026-09-14 P2 首批开发配置冻结（未运行结果）

P1 已完成且独立复核通过后，新增 `configs/v43/p2_first_dev.yaml`、`cli p2` 与 A4/A5 正式接线。只取 ETTm1 / Solar / USTS 的 dev 原始区间，分别 14 / 11 / 1 个不重叠基础 parent；两个 horizon 96/192 及 raw、target block 10%、全部 siblings shared block 10% 共 156 个 episode。这些变体不是 156 个独立样本；全历史 ridge 读取区间在 dev 内重叠，正式推断还须按更大依赖块处理。

Solar 复用本机文件，与论文作者仓库 Git blob 完全一致。只按原行号保留同步时间；来源说明为 2006 年 Alabama 137 路 10 分钟光伏，压缩文本无原始时间戳，不能伪称已恢复绝对日历或发布延迟。库存只统计行宽，不解码 heldout 数值。来源证据见 `docs/v43_p2_source_provenance_20260914.json`。

首批重点检验长缺口的新信息收益，以及原始/共享缺失的保护与负对照；5%/点缺失/spike/valid-event 标注和其他来源为后续矩阵，不能把首批当完整污染实验。固定通道0、seed101、L512、缺口[230,281)、不按标签挑窗口。只要求 TS-ICL/Bolt 两个已通过模型，保持 batch1/GPU单任务。

执行臂为 A0 native/ffill、A2 single、A3 官方全合法covariates、A4当前context及同split全合法历史ridge、A5严格嵌套OOF静态eta。A4历史只读 dev 起点到当前context起点，再拼当前dirty输入，不重新打开当前gap真值；这是额外历史信息轨道。A5保存七类真实遮挡输入，支持不足明确回到同origin已验证A2，真实worker缺行/失败仍报错；eta平局取0。候选去重只在同episode、同horizon、完整输入hash下进行并保存alias，所有候选完成真实预测后才读future。

A1仍为blocked_adapter：`experiments/v39_phase0_replay.py:321` 强依赖771个冻结parents，`:450`按历史候选标签拟合LODO PICS并重放；源码存在不等于可对本次新时间区间部署。该参照待合法适配，不把旧分数贴到新UID，也不把缺失参照填0。原生多变量任务模型尚未运行。A9只对本轮完整已执行候选集生成开发上界，名称明确为A9_ORACLE_AVAILABLE，不冒充全候选上界。

新增测试覆盖gzip越界标签不可解码、父区间共享、shared辅助遮挡、全历史不重读gap、A5不适用回A2、漏真实预测报错、ridge观测值不变及eta平局0。运行以新配置绑定的CPU gate为前提，结果产出前不作方法成功结论。PICS_joint_relabel不变，不将规划写入DOCX成果。


### 2026-09-14 21:20 可选模型元数据恢复

仅对可选 Chronos-2 元数据使用子进程 `NO_PROXY=*` 直连，3.27秒成功固定 revision `29ec3766d36d6f73f0696f85560a422f50e8498c`；未下载权重，模型仍metadata-only/pending。当前bootstrap manifest为completed，TS-ICL/Bolt保持ready；旧orchestrator退出码1和两次TLS失败不改写。完整旧record/manifest及新恢复状态保存于logs/v43/chronos2-metadata-direct-20260914，摘要见docs/chronos2_metadata_recovery_20260914.json。代理服务和Codex配置未改，P2使用运行开始时冻结的必需模型manifest。


## 2026-09-14 21:20 post-hoc：P2首批真实实验完成

51项CPU测试通过，H96/H192、三个dev来源、26个基础parent/156变体，512次真实插补、580次去重预测、1092份任务标签全部完成，独立原始结果复核通过；耗时251.386秒，峰值GPU分配1.90GB。KEEP/A2/A5来源宏平均MASE为1.258454/1.157005/1.157187，无可靠确认性CI。A5仅4个ETTm1 parent产生8个修正变体，未来任务2好6坏；其真缺口重建6好2坏。加入A5后，相对已含简单跨通道强对照的oracle增量为0，不能晋升残差方法或进入更大A8训练。A1与原生多变量对照待适配，其他污染条件/来源尚未覆盖；calibration/test读取仍为0，PICS_joint_relabel不变。全部状态、误差分歧与成本见docs/v43_p2_report_20260914.md和docs/v43_p2_evidence_20260914.json。


## 2026-09-14 22:00用户新验收与A5遗漏候选诊断

用户明确将验收聚焦于因数据而异治理、合法证据的任务判别能力、主动获取相对固定流程的收益。TS-ICL的8.06%保留为基线收益，不归于agent；A5静态失败不能推断完整agent无效。优先级从补算子/A1转为现有固定候选池的最小agent对照，见docs/v43_agent_acceptance_20260914.md。附件第24节原文件当前不可见，本机第24节仅为本轮消息的明确要求摘要。

复用全部已有TS-ICL遮挡输出，为未选择的eta强度补92次真实Bolt预测（20.16秒，未新增future读取），50个受支持episode中发现26个变体/14个parent有静态规则错过的更优任务强度。相对固定五臂强候选池，全部eta的开发oracle仅从MASE1.094080降至1.093753，增量约0.000327；新增胜出变体ETTm1有4个、Solar有5个。此前“加入A5的oracle增量为0”仅适用于静态选择后的候选，不适用于所有eta。保留两种口径和原始失败，不将新oracle作为部署成绩。证据见docs/v43_a5_extended_diagnostic_20260914.json。

最小agent配置configs/v43/agent_minimal.yaml已冻结：110个train parent按时间拆75 scorer-fit / 35 acquisition-fit，dev维持26 parent。固定五臂初始池，strict_mask/history_probe两个验证工具，最多两轮；比较全体固定臂、train最佳固定、dirty简单选择、mask/history规则、全部证据、两种固定顺序及学习获取停止。同一浅层HGB模型族，source/uid/seed/真实缺陷类型/future不得进特征。费用计入基础候选、验证、选择、最终预测与分片加载/IPC，固定臂仅计实际所需候选；真实在线按需核验在离线评估后执行。尚未运行得出的agent结果均为pending，不作ICLR或晋升声明。


## 2026-09-14 最小 agent 开发验收：H1 有选择空间，H2/H3 未通过

已完成固定五臂、两个验证工具、19组策略：110 train parent按时间拆75动作评分/35工具获取，dev为26 parent/156变体。58项CPU gate、16,272份真实模型输出和2,964条决策/费用独立复核通过。五臂oracle MASE1.094080、train最佳固定TS-ICL1.157005、dirty简单选择1.165414、学习获取停止1.190112、全部调用1.204589；当前agent没有超越强固定/简单策略。173次实际工具步骤中156次任务误差不变、6次改善、11次恶化，mask证据未改变当前评分器动作，历史证据在Solar造成明显退步。

A5补全诊断证明100个非零eta候选均改变真实预测；相对静态有26个更优替代，其中20个为遗漏非零修正、6个应回eta0，不能全称作有效修正被拒绝。对强五臂池的oracle增量仅0.000327。A5新增92份预测、150标签及三种oracle池已独立复核；首次复核未解析旧预测alias而失败，修正映射后通过，保留失败。原P2“54个shared”纠正为52 shared + 2 USTS raw。

低预算学习和history-first各1/156变体超支，未删样本或截费用。初版在线过早加载离线标签档案、1例冷调用轨迹不同均保留；修正后文件访问屏障封锁6个evaluator/缓存档案，7例动作/证据/最终预测一致。完整在线进程24.661710秒，7请求摊销3.523101秒，含初始化/退出；137份在线模型输出与7条实际费用轨迹独立复核通过。批量0.4703秒的学习策略含最终预测费用不是冷在线成本。

PICS_joint_relabel不变；TS-ICL约8.06%是基线收益，非agent。calibration/test读取0；无ICLR/SOTA/安全保证。下一步只在train内注册证据状态独立评分、H32→H96/H192收益排序迁移、无增益工具停止和真实成本预算诊断，先满足H2/H3再扩强baseline/第二TSFM/独立确认。RED不写入DOCX已验证成果。

完整报告与代码证据：[v43_agent_report_20260914.md](v43_agent_report_20260914.md)，机器记录：[v43_agent_evidence_20260914.json](v43_agent_evidence_20260914.json)。原始run `results/v43/20260914T141030.324186Z-agent`；最新阶段状态 `results/v43/agent_stage_acceptance.json`。运行Git HEAD为c92eab5上的未提交工作区，先按内容hash冻结，随后9f5f49f保存了完全一致的代码；不以旧HEAD覆盖快照。
