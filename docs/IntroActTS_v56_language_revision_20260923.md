# v56 原入口语言与逻辑修改记录

入口未更名：`latex/IntroActTS_20260923_v56.tex`。修改前备份位于 `archive/paper_20260923/pre_language/`，包含源码、PDF与文献库。备份PDF留在本机，不作为实验产物提交。

## 实际修改

- 重写摘要、引言、问题定义和结论，建立请求级修复问题、历史效用估计、执行决策和实验证据之间的对应。引言单列Challenges与Contributions，简短Related work指向附录，第二节直接定义任务。
- 定义可用动作集合与期望损失目标，区分任务约束和方法步骤。核权重带宽改记为h_a，避免与执行阈值τ混用。选择明确限定于可用动作，无可用修复时返回KEEP。
- 分别解释主实验、门控、消融、严重度与成本。保留Source Fixed在Bolt上的优势、去门控降低MASE但增加伤害、部分配对区间含零及Exchange敏感性，不将均值排序写成普遍显著优势。
- 根据v55结果文件替换过时的五基线Holm表，当前为九基线乘三个骨干的27项保存统计量。其余49张表的数值保持备份一致，允许行重排和标记修正。
- 确认集Chronos-2最优应为Source Fixed，次优为R2-CART，已修正。星号明确表示Holm校正后的p值，而非声称名义区间已被Holm调整。
- 依据selection记录，将一个标准误带内配置数写为11、6、29，更新当前与严格最小值配置，披露先查看后者TEST结果再采用一个标准误规则的顺序。最小值结果已从本地同步档案读到。
- 旧工作点的惩罚扫描、得分诊断、机会分层和成本核算明确标为历史结果，避免与当前1.425、55.2%、0.0340混用。
- 实测成本保留验收后的80请求、零合法性不一致结果，同时披露payload中的限制：SAITS计时来自实时前向，而候选值来自归档结果，不称为原始SAITS输出的全新端到端复现。
- 增加硬件事件验证尚未完成的准确说明。未运行新实验，未改变任何预测或原始结果文件。
- 完整英文与逐段中文对照覆盖294个文字块和50张共用表格，保留英文引文命令，正文与附录均包含在内。

## 依据与发现

用户提供的验收报告为 `docs/v55_acceptance_20260923.md`，提交b9893fe。主要依据为 `results/v55/evaluation/`、`protocol/`、`confirmatory/`、`gate_controls/` 和 `results/v54/cost/e2e_latency.json`。

验收报告写TEST交叉校准9/12达标，原始结果的respected字段实际为Bolt 3、TimesFM 2、Chronos-2 3，共8/12。稿件使用8/12。Bolt α=0.02的实际值为0.020133，Chronos-2 α=0.01为0.010170，不能因显示舍入而算作达标。原报告未覆盖重写，发现记录于此。

## 验证

- 原入口完成LaTeX、BibTeX及交叉引用编译。
- 正文结束在第9页，全文41页。AI声明在结论之后。
- 无缺失引用标签、无Overfull、无未定义字体警告。
- `v55_audit_numbers.py`：64/64记录值在稿件中命中，E3合法性不一致为0/80。该检查是保存值与稿件的核对，不是独立实验复现。
- `audit_layout_v56.py`：49张表数值保持一致，27项Holm比较对应保存结果，中文对照源文件哈希匹配。
- 渲染检查正文九页及更新的统计、确认、成本和附录末页。主表、消融表通栏对齐，组图保持2×3且占满左右，未重新设计作者图1和图2。

本轮未重新联网核查全部文献，也未核验ICLR 2027最新投稿政策。未完成的硬件预测级验证与精简状态重新调参不标作完成。
