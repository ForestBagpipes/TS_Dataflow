# 下一轮独立执行提示词：r5负结果收口与独立评估准备

请接管 `/home/vipuser/work/work2` 的 IntroAct-TS，始终使用简体中文。沿当前真实HEAD及后继修改接续，先读AGENTS.md、HANDOFF、version_ledger、v431_r5_report/main_table/mechanism_table/verification、实际工作区及进程。保留他人修改、r5冻结模型与全部失败，不重置、不强推、不重装健康环境。

方向保持面向冻结TSFM的不完整输入治理：五臂Native KEEP、FFILL、单变量TS-ICL、多变量TS-ICL、context ridge；有效观测不覆盖，完整target直接KEEP。最终输出来自真实执行过的一个版本，不改预测器路由、融合、残差修正或TSFM训练。

已完成r5：当前任务预测响应的ridge评分、单臂/成对/完整联合收益投影、最多三个版本预算执行、严格可见状态在线运行。模型冻结hash为`6bac8291c9c06eca4cb2c6f9a9b6fe49373d51815ff302421fc92b70c6d12792`，配置和源码hash在`results/v431-r5/fit/models_frozen.json`及manifest；如文件缺失先按审阅包真实映射查找，不能杜撰已有缓存。

固定事实：旧DEV26parent/156相关变体。高预算Bolt r5=免费修复1.152415，TRAIN固定流程1.154554，DIRECT_control1.146635，继承R2 CART1.136486；TimesFM r5 1.074244，免费/固定流程1.069398，R2 CART1.069086。R2信息不同。固定同查询FULL−RAW：Bolt−0.001197，90%描述区间[−0.003590,0]；TimesFM+0.000418，[+0.000128,+0.000926]。收益向量误差下降但任务收益条件未通过。Bolt改善集中同一个parent，不得当作稳健新贡献。

每家族fit54parent/324变体，gate21parent；严格同来源forward/purge参考OOF中35个fit块历史支持不足16，走登记回退。110登记TRAIN不等获取有效样本。旧DEV/check/acq/金融均已查看，不是确认集。calibration/test保持封存，PICS_joint_relabel保留历史协议身份。

真实在线每家族44请求，38自然：Bolt0自然取证，TimesFM22次；各1次低预算超支，TimesFM1次求解超时回退。高预算自然无超支。全部最终预测对应已执行版本；零预算和取证后故障受控分支保留费用。不要将自然取证称自然缺口。金融只有Brent现货报价和Kim–Wright拟合远期利率2parent/12变体，部分重叠旧DEV；Bolt KEEP2.820959强于r5 3.042476，TimesFM KEEP5.225872、r5 4.890106、免费4.890237。自然缺口/完整历史vintage尚未验证。

本轮r5准入失败，停止增加算法配置、特征、lambda或骨干选择。不要打开确认集挽救开发。以下工作已经授权自主执行：

1. 复核最新独立审计的剩余具体问题，仅修工程风险；若影响输入、费用或决策，保留原表并单列修复重放，不在DEV调参。显式区分组件账本、真实单请求和冷启动费用。父队列锁与worker gpu.lock不同，避免重入冲突。
2. 完成8来源主实验准备：ETTh1/2、ETTm1/2、Electricity、Exchange、Traffic、Weather。依据元数据/许可与可用性登记来源和未用时间块，不按胜率选。沿已有边界核验与旧TRAIN/DEV/pilot/check/acq完整读取区间重叠；不读封存标签。当前target channel0与常见ETT OT协议不同，原任务不偷偷改目标，官方协议另表。几何容量不是已验证样本数。
3. 补齐公开数据manifest、任务mask、来源与parent权重、评分脚本、原始预测和成本存储协议。完整输入与受控缺口分轨；自然缺口必须有日历/发布/可用性证据，无证据就明确缺项。金融不平滑真实跳变，不把休市填成缺失。
4. 优先完成TATO官方场景级TRAIN搜索与冻结部署入口验收，按实际资源登记500实例/500trial的执行范围、预算及ETA。现有两家族8trial仅短预算适配。旧TimesFM546/1248长度失败来自trimmer长度超过320/416，保留失败，不静默缩空间后称完整复现。官方原协议与受限治理协议分别报告。
5. TimesFM-3、Chronos-2仅做最新许可、依赖、显存、原生缺失与模型revision登记；未实际执行不列成绩。不借升级骨干掩盖r5治理增量失败。新增GPU实验按队列和已登记协议执行，先少量合法TRAIN测真实吞吐，不用其表现挑方法。
6. 维护论文：当前只支持可执行系统、无需未来标签的收益约束和评分误差改善但排序可恶化的负结果。投影、三角不等式、FW、预测特征、任务导向治理和获取停止都有先例。不得声称主动优势、金融泛化、独立确认、SOTA或录用保证。

全部训练、推断、下载、测试和统计在当前服务器执行。先source scripts/env_new_server.sh，检查GPU/CPU/RAM/磁盘/其他任务；复用隔离环境，单项重GPU。不得终止他人任务、改驱动/全局代理或购买资源。只做当前风险相关测试。

每批保存实际命令、配置、模型与数据hash、角色支持、原始预测、费用、失败和状态。更新HANDOFF、台账、claims、novelty、实验矩阵、论文及累计研究计划，保留旧报告。检查git状态与凭据/数据大文件后用codex-save-local提交并推送当前codex分支，记录本地和远端SHA，不声称已观察到Windows同步。

用户无法同步服务器：每个关键批次直接在聊天完整报告结论、共同表、同信息机制、全部成本、超支/失败/回退、金融各条件、代码归因、论文可支持主张、真实Git状态、下一队列及缺项，不能只发路径。结束时再贴一份完整可独立执行的接续提示词。
