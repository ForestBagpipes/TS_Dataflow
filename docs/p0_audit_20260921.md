所有 12 项的证据收集已完成。说明一点：我作为只读 subagent 没有文件写入工具（角色约束禁止使用 Bash 写文件），因此无法直接落盘 `docs/p0_audit_20260922.md`。下面是完整核查报告全文，主 agent 可直接将其保存为该文件。

---

# IntroAct-TS v52 投稿稿 P0 疑点核查报告（2026-09-22）

核查对象：`latex/IntroActTS_20260921_v52.tex`（2291 行）；证据：`results/v47/`（本地+服务器 `/home/vipuser/work/work2`）、`src/introact_ts/v44/`、`src/introact_ts/v47_verified/`、`scripts/v47_*.py`、`configs/v47-verified/protocol.json`。表编号按 tex 出现顺序推算（Table 11=`tab:app-transfer`，13=`tab:app-holm`，29=`tab:app-pattern-res`，30-32=`tab:app-sev10/30/50`，34=`tab:app-recutils`，40=`tab:app-seeds`，41=`tab:app-efficiency`，42=`tab:app-calls`，46=`tab:heterogeneity`）。

## 逐项结论速览

1. 邻域诊断：**已确认无误**（v52 文本与表均已改为非单调表述，数字与诊断 JSON 逐位一致）。
2. Table 40 口径：**已确认无误**（R-002 修复路径二已落实，文中已改为双 horizon 子集）。
3. 平均排名口径：**需修正文字**（实际为逐 episode 竞争排名、1e-12 并列容差、NaN 逐 episode 剔除；Table 29 排名列与 caption 的自然读法不符；Table 1 "cell by cell" 措辞不准）。
4. 缺失率描述矛盾：**需修正文字**（A.5.1 的"哈希抽样 severity"是未使用的 v44 `bank` 块旧语义；论文实际用 `bankx` 逐一枚举）。
5. Table 34 vs 46：**需修正文字**（Table 34 全表是 Bolt-only 未声明；Table 46 数值是三骨干宏平均但 episode 计数列是单骨干口径）。
6. Table 41 延迟：**需重算或改写**（SAITS/TATO 两行完全相同是因为都只复用公共 backbone 调用计时的三骨干宏平均，方法自身服务耗时时从未测量；IntroAct 行是算术合成而非实测；cold start 2.6 s 无记录支持）。
7. Table 6 缺失位置计算：**需修正文字**（缺失位置差值被 `nan_to_num→0`，四个幅度类 intervention 特征在守约动作下恒为 0，只有 changed fraction 携带信号——即已知 L-003）。
8. KEEP 骨干缺失处理：**已确认实现，但论文未写明**（三骨干均原样接收 NaN，处理方式各不相同，建议补骨干级说明）。
9. Table 13 统计：**已确认无误**（区间/p/Holm 同批记录，方向、单位、校正范围全部对上）。
10. SAITS 交叉拟合：**已确认无误**（真 out-of-fold、2 折，文稿已改对；仅"按 origin 时间分两半"措辞与实际的交错分配略有不符）。
11. 数据用途：**已整理成表**（见下文草稿；`bank`/`bankx2` 在论文管线中未使用）。
12. Table 40 Δ 方向：**已确认无误**（FULL−KEEP，负值=改进，与 Table 13 口径一致；建议补一句符号说明）。

---

## 1. §3.2 / A.4 / Table 11（`tab:app-transfer`）邻域诊断

- 论文位置：§3.2 第 260–262 行；A.4 第 935–946 行；表在第 953–979 行。
- 当前表述：正文 "Distance has only a weak, nonmonotone relationship with estimation error"（262 行）；附录 "estimation error does not increase monotonically with neighbourhood distance"（944 行）。**旧稿"最远邻域误差最大"的说法在当前 tex 中已不存在**。
- 证据：`results/v47/diagnostics/transfer_test_{bolt,timesfm,chronos2}.json` 的 `bins` 与 `spearman_distance_vs_error` 与表中全部 15 个单元格及三个秩相关（0.094/0.011/0.057）逐位一致。TimesFM 最近组误差 0.559 确大于最远组 0.205，Chronos-2 中间组 0.306 高于最远组 0.271——表如实呈现了非单调性。
- 结论：**已确认无误**。无需改动。

## 2. E.3 / Table 40（`tab:app-seeds`，1933–1974 行）H=96 口径

- 当前表述：1943–1944 行已写 "patterns P1 to P4, $10\%$ severity, $H \in \{96,192\}$"，caption 已写 "recorded dual-horizon subset"。R-002 的修复路径二（改文字为双 horizon）已落实。
- 证据：`results/v47/evaluation/{test,test_m2,test_m3}_{bolt,chronos2}.json` 的 `rows.FULL_INTROACT`：MASE 1.3783/1.3806/1.3762（Bolt）、1.3779/1.3728/1.3805（Chronos-2），Δ vs KEEP −0.0881/−0.0804/−0.0867、−0.2014/−0.2040/−0.2218，IR 0.746/0.761/0.759、0.691/0.682/0.691——与表逐位一致。Spread 行：Bolt MASE 0.0044→0.004 ✓、IR 0.01447→1.4% ✓、Chronos-2 0.0077→0.008 ✓、0.0092→0.9% ✓。正文 "simple selector moves by 0.059 / 0.022" 亦对上（R2-CART 三次 MASE 1.4022/1.3989/1.4579 与 1.3846/1.3622/1.3715）。
- 结论：**已确认无误**。表中数字确为双 horizon 口径且文字已匹配。可选改进：caption 可附一句 "H=96-only 值为 Bolt 1.1731/1.1699/1.1861、Chronos-2 1.1106/1.1148/1.1109" 以备审稿人追问。

## 3. Table 1 / 29 / 30–32 平均排名口径

- 论文位置：Table 1 caption 436–439 行（"over the deployable rows of this table, cell by cell"）；Table 29 caption 1560–1562 行（"the average rank over the four patterns"）；Table 30 caption 1593 行（"Ranks are over deployable methods only"）；Table 31/32 caption 未重述排名口径。
- 实际实现（关键）：产出 `results/v47/evaluation/*.json` 的是**服务器上的 `scripts/v47_evaluate.py`**（本地未同步，315–385 行）——逐 episode 排名、**并列用竞争排名（1224，容差 1e-12）而非平均秩**、每 episode 内 NaN 的方法被静默剔除（`if np.isfinite` 过滤，少于 2 个有限值的 episode 整行跳过），缺少 baseline 时 `continue` 排除出池且不记录。注意这与验证版 `scripts/v47_evaluate_verified.py:366-372`（scipy 平均秩、非有限即报错、记录 `missing_baselines`）语义不同。
- 数值核对：Table 1 的 Avg. rank 列 = 三骨干各自 760 episode 平均秩的宏平均，全部对上（KEEP 3.36 / BF 2.94 / R2 2.97 / SAITS 2.98 / TATO 4.10 / FULL 2.79）；正文 429 行 "lowest mean rank … on each backbone" 逐骨干成立（Bolt 2.821 vs BF 2.824 极接近）。Table 30–32 的 Rank 列与 test/test30/test50 三文件宏平均逐位一致 ✓。全部 9 个 block×骨干组合排名池均为完整 6 方法（TATO 无缺失）。
- Table 29 问题：排名列（3.36/2.94/2.97/2.98/4.10/2.79）与 Table 1 完全相同，是逐 episode 排名。按 caption 自然读法（对四个 pattern 列的值做排名再平均）复算得 4.12/4.12/4.25/3.25/2.50/2.75，**不符**。但存在一种等价读法使其成立："在每个 pattern 的 episode 子集内算逐 episode 平均秩、再对四 pattern 宏平均"——因每 pattern 恰好 190 个 episode（95 parents × 2 horizons，等权），该量与总体逐 episode 平均秩数学上恒等。
- 结论：**需修正文字**。建议：①Table 1 caption 把 "cell by cell" 改为 "per request, averaged over the 760 requests of each backbone and then over backbones; ties share the better competition rank; the oracle is excluded"；②Table 29 caption 明确排名是 "per-request ranks computed within each pattern and macro-averaged over patterns（等价于主表口径）"，否则按四列值重算；③Table 31/32 caption 补一句排名口径沿用 Table 30；④披露并列规则（竞争排名、1e-12 容差）与逐 episode 剔除规则。

## 4. A.3.1 vs A.5.1 缺失率描述矛盾

- 论文位置：A.3.1（`app:replay`）884–886 行 "Pseudo-deployment severities enumerate {10%, 30%, 50%} on every parent, pattern and horizon"；A.5.1（`app:patterns`）991–993 行 "The same key determines the pseudo-deployment severity that the replay uses, which is drawn from {10%, 30%, 50%}"。
- 证据：`src/introact_ts/v47_verified/grid.py:53-70`——`bank` 块 `None`（哈希混合 severity），`bankx`/`bankx2` 枚举全部三级，`train_eval/test/test_m2/test_m3` 仅 10%，`test30/test50` 各一级。论文管线实际用 `bankx`：`results/v47/protocol/selection_bolt.json` 的 `block="bankx"`、5352 episodes = 223 parents × 4 patterns × 2 horizons × 3 severities（枚举）。哈希抽样只存在于 `mixed_severity`（`src/introact_ts/v44/masking.py:193-207`），且用独立的 `:severity` 哈希命名空间——并非"同一个 key"；mask 位置种子（`mask_seed`）本身不含 severity，severity 只经 `_severity_budget` 影响删除预算。
- 结论：**需修正文字**。A.3.1（枚举）与事实一致；A.5.1 991–993 行描述的是未被使用的 v44 式 `bank` 块。建议改为："The mask of an episode is a pure function of (source, parent, origin, horizon, pattern, protocol_seed); the severity sets the deletion budget. The replay bank enumerates all three registered severities on every parent, pattern and horizon.（哈希抽样 severity 的 `bank` 块属于历史协议，未用于本文任何数字。）"

## 5. Table 34（`tab:app-recutils`）vs Table 46（`tab:heterogeneity`）

- 论文位置：Table 34 在 1714–1740 行；Table 46 在 2180–2207 行。
- 证据：Table 34 的全部数值（Rec MSE/MAE、平均效用 −0.139/+0.013/+0.029/+0.043/+0.037、episodes 760/760/758/566/747、winner agreement 19.3%、discordant 44.5%、Spearman 0.12）逐位等于 `results/v47/diagnostics/reconstruction_test_bolt.json`——**整表是 Bolt 单骨干**，caption 只说 "the reused evaluation episodes"、"the same backbone"，未点名 Bolt。Table 46 的 Mean utility 与 Best/Beneficial share 经核对是**三骨干宏平均**（如 FFILL (−0.1388−0.0174−0.1271)/3=−0.094 ✓，KEEP best share (0.211+0.161+0.209)/3=19.3% ✓），但 "Available episodes" 列 760/760/758/566/747 是**单骨干计数**（三骨干该列相同，因为动作可用性与骨干无关）；若真按 caption "all three backbones" 汇总应为 2280/2280/2274/1698/2241。两表效用差异（如 SAITS +0.037 vs +0.101）即 Bolt-only vs 三骨干宏平均之差，文中未说明。附带风险：Table 46 脚注的 "19.3% / 44.5% / 0.12" 是 Bolt-only 的重建-效用一致性统计，与同表 KEEP best share 19.3%（三骨干宏平均）数值巧合相同但含义不同；且 Figure 6 的单元格是三骨干宏平均（`latex/figure/data/rank_agreement.csv` 与三骨干 `rank_grid` 均值一致），与脚注引用的 Bolt-only 统计总体不一致——若改取宏平均应为 20.6% / 43.9% / 0.14。§4.2 第 398–400 行引用的也是 Bolt-only 值且未声明骨干。
- 结论：**需修正文字**。建议：①Table 34 caption 与 §4.2 明确 "on Bolt"；②Table 46 caption 改为 "macro-averaged over the three backbones; the episode counts are per backbone and identical across backbones because action availability is backbone-independent"；③脚注的三个一致性统计统一口径（声明 Bolt-only，或换宏平均值并同步 Table 34 脚注与 §4.2）。

## 6. Table 41（`tab:app-efficiency`，1988–2009 行）延迟复用

- 证据：`results/v47/cost_audit/latency_test_{bolt,timesfm,chronos2}.json` 仅有 `retrieval_per_request`（2.34/2.27/2.29 ms）与 `backbone_call`（mean 45.41/103.90/19.53，p95 54.47/110.23/21.58，max 474.11/321.74/368.56 ms）。三骨干宏平均 = **56.28 / 62.09 / 388.14 ms —— 正是 SAITS 与 TATO 两行的 56/62/388**。两行完全相同的原因即二者都只是"每请求 1 次 backbone 调用"的公共计时宏平均：SAITS 的插补前向、TATO 的管线变换在服务端从未被单独计时（`results/v47/replay/saits/report_*.json` 只有训练/批量插补总时长；`results/v47/baselines/tato_test_*/records.json` 无逐请求计时字段）。IntroAct 行是算术合成而非实测：mean ≈ 56.28×(1+0.654)+2.3 ≈ 95 ✓，p95 ≈ 2×62.09+2.4 ≈ 127 ✓，max ≈ 2×388.14+5.2 ≈ 781 ✓（注意 mean 用 1.654 次调用、p95/max 用 2 次，内部口径不一致）。offline 列核对无误：TATO 3072 = 48 trials × 8 windows × 8 sources ✓（`tato_search_*.json`），SAITS "8+16、247 min" 与 8 份 report 的 runtime 总和 247.3 min ✓，IntroAct 30,160 = bankx `status.json` 的 `unique_predictions` ✓。Table 42 的 3,800 = 760×5 ✓（同为单骨干计数配宏平均措辞）。**"Cold start: 2.6 s" 无任何记录支持**：三份 forecast `status.json` 的 `load_seconds` 为 7.3/3.7/6.5 s；"first request of a process: 12" 亦未见出来源。
- 结论：**需重算或改写**。建议：①两行相同的单元格加注 "backbone-call time only, macro-averaged over backbones; method-specific serving time (SAITS imputation pass, TATO pipeline application) was not separately instrumented"，或补测；②IntroAct 行标注为合成上界并统一调用次数口径；③cold start 改为实测加载时间（7.3/3.7/6.5 s 或均值 5.8 s）并说明 12 的计数来源，否则删除。

## 7. Table 6（`tab:app-features`，734–777 行）X^a − X^{a_0} 的缺失位置处理

- 证据：`src/introact_ts/v44/state.py:197-228`（`intervention_features`，v47 管线经 `src/introact_ts/v47_verified/catalog.py:33` 直接复用）：`changed = ~np.isclose(candidate, reference, rtol=0, atol=0, equal_nan=True)`（双 NaN 视为不变）；`delta = where(changed, candidate−reference, 0)` 再 `nan_to_num(..., 0)`。由于目录动作按契约不改观测位（`actions.py` 各分支均 splice 回原值），观测位 delta≡0；缺失位 reference 为 NaN，candidate−NaN=NaN→0。**后果：mean/max/change-trend/near-origin 四个幅度特征对所有守约动作恒等于 0，只有 changed fraction（= 动作填补的位置占比）携带信号**；标准化时零方差特征 scale 置 1（`select.py:93-94`），故这四个特征对检索距离无贡献。此即修正报告登记的 L-003。效用参照本身没问题：`catalog.py:229` `utility = reference_loss − entry.mase`，与论文 (eq:app-gain, 875 行) 一致。
- 结论：**需修正文字**。可写入论文的准确描述："The difference is taken position-wise with exact equality; positions missing in the reference enter the difference as zero (the candidate fills them, but the reference has no value to subtract), and positions an action leaves unfilled count as unchanged. Since every catalog action preserves observed entries by contract, the four magnitude features vanish identically and only the changed fraction varies across actions.（或：修复实现使缺失位以填补量参与差值后重算 A2/检索。）" 同时 Table 6 caption（730–731 行）自称 "including … the missing-value handling of each feature"，intervention 组实际未写，需补。

## 8. §3.1 KEEP 的骨干级缺失处理

- 证据：KEEP 原样交出含 NaN 的输入（`src/introact_ts/v44/actions.py:204-206`，`strategy: "native_nan"`）。骨干适配层（`scripts/v431_baselines/worker.py`、`scripts/v46_chronos2.py`）均不做任何填补：Bolt（chronos-bolt-base，bf16）直接把含 NaN 的 context 交给 `ChronosBoltPipeline.predict`（Chronos 系官方支持 NaN 掩码）；TimesFM-2.5 走官方输入管线，其原生行为为"context 内线性插值 + 前缀 NaN 剥离"（记录于 forecast `status.json` 的 `identity.native_nan_behavior`；适配层仅拒绝 inf/全缺失输入）；Chronos-2（fp32）直接传入 NaN（Chronos-2 原生支持缺失值）。三者都在输出非有限时抛错并记为失败，绝不静默修复。论文目前只有 roster 表 1250 行 "hands the incomplete context to the frozen backbone"，无骨干级说明；且只有 TimesFM 的 identity 记录了该行为，Bolt/Chronos-2 的 status.json 未记。
- 结论：**实现已确认，论文需补写**。建议在 A.5 或 roster 表加一行/一段："KEEP 依赖各骨干的原生缺失处理：Bolt 与 Chronos-2 直接接受 NaN context（官方掩码语义），TimesFM-2.5 由其官方输入管线做 context 内线性插值并剥离前缀 NaN；适配层不做任何额外填补，非有限输出记为失败。"

## 9. Table 13（`tab:app-holm`，1028–1057 行）统计一致性

- 证据：`results/v47/evaluation/test_*.json` 的 `comparisons` 段与表内全部 15 行的差值、区间、raw p、Holm p 逐位一致。实现 `src/introact_ts/v47_verified/select.py:351-408`：parent-cluster bootstrap（源内重抽 parent，源宏聚合一并进入每次重抽）、2000 次、seed 101（与 `protocol.json` 一致）；区间与 p 值取自**同一批 draws**（p 为双侧、下限 1/2000=0.0005，表中 0.0005/0.001 均符合）；Holm 族 = 每骨干 5 个预声明比较（KEEP/BF/R2/SAITS/TATO），消融不入族——与 1018–1024 行文字逐项相符；差值方向 FULL−对照、负值利于本方法，caption 已声明。
- 结论：**已确认无误**。

## 10. P0-3 SAITS 交叉拟合

- 证据：`scripts/v47_saits.py:50-53,101-107,173-203`——`FOLDS=2`；fold 按 origin 排序后**交错分配**（`i % 2`）；对 fold f，用 fold≠f 的 parent 训练、只插补 fold==f 的 episode，确为真 out-of-fold；fit 块为 `bankx`（枚举 severity）。8 份 report（crossfit s1–s4 各 2 folds、full s1–s4）与 8+16=24 个模型、247 min 对上。deployment（full）模型在全 bankx 上训练后服务 train_eval/test/test30/test50/test_m2/test_m3。论文 689–690 行 "uses two folds. Four-fold cross-fitting belongs to the separate verification revision" 与实际一致（`configs/v47-verified/protocol.json` 的 `folds: 4` 属于验证版配置）。
- 结论：**已确认无误**，两处小注：①1289 行 "split in two by origin time, each half trains a model" 与实际交错分配（非连续两半）措辞略有不符，建议改 "split into two interleaved folds by origin order"；②crossfit 只覆盖 `bankx`（服务器 `results/v47/replay/saits/` 无 bankx2 数组），验证版若以 bankx+bankx2 为库则 bankx2 的 SAITS 动作无支持——与论文数字无关，但值得在验证记录中登记。

## 11. P0-1 数据用途表（草稿，可直接入附录/台账）

依据 `grid.py:51-70,142-166`（`assign_train`：每个 source 内按 `read_start` 排序，前 80%→bank、后 20%→train_eval）、`configs/v47-verified/protocol.json` 及各结果目录：

| 块 | parents/episodes | 掩码与 severity | 用途 |
|---|---|---|---|
| `bank` | 223 / –（未建） | 哈希抽样 severity（v44 旧语义） | **未用于本文任何数字**；A.5.1 现行文字描述的是它，需改 |
| `bankx` | 223 / 5352 | 主种子掩码 × 枚举 {10,30,50%} | 回放库、特征标准化统计、baseline 训练（SAITS 部署/交叉拟合、TATO 搜索 8 窗）、k/β 的 LOPO 选择（`selection_*.json`） |
| `bankx2` | 223 / –（未建） | 种子+1 掩码 × 枚举 | 仅验证版 runner 引用；保留论文管线未构建 |
| `train_eval` | 53 / 424 | 主种子、10% | 冻结前一次验收检查；v52 门控对照的开发干预率匹配与消融开发侧读数 |
| `test` | 95 / 760 | 主种子、10% | 主表与全部主文数字（post-hoc，历史参与过开发，§5 已披露） |
| `test30` / `test50` | 95 / 760 各 | 主种子、30%/50% | 注册网格内 severity 稳健性（Table 31/32、图 5） |
| `test_m2` / `test_m3` | 95 / 760 各 | 种子+1/+2、10% | 掩码实现稳定性诊断（Table 40），不做显著性检验 |

purge：TRAIN/TEST 行区间间隙逐源为正（`audit_purge`），源内相邻 parent tile 于 L+max(H)、跨块边界另有 purge 校验。

## 12. Table 40 "Δ vs KEEP" 方向

- 证据：Δ = FULL−KEEP（同行块同实现），数值 −0.088/−0.080/−0.087、−0.201/−0.204/−0.222 与 `evaluation/*.json` 逐位一致；负值=改进，与 Table 13 caption "A negative difference favours IntroAct" 同口径。
- 结论：**已确认无误**；建议在该表 caption 补 "negative favours \introact{}" 并去掉 Δ 列的 ↓（有符号差值配 ↓ 易被误读为绝对值）。

---

## 返回摘要（每项一行）

1. 邻域诊断：**已确认无误**——v52 文本已是"弱、非单调"表述，表与 `results/v47/diagnostics/transfer_test_*.json` 逐位一致。
2. Table 40：**已确认无误**——R-002 修复已落实为双 horizon 口径，6 个 MASE/Δ/IR 及 spread 全部对上。
3. 排名口径：**需修正文字**——实际为逐 episode 竞争排名（1e-12 容差、NaN 逐 episode 剔除，服务器 `v47_evaluate.py`）；Table 29 排名列只是 Table 1 的复制，caption 的自然读法不成立；Table 1 "cell by cell" 不准。
4. 缺失率矛盾：**需修正文字**——论文实际用 `bankx`（逐一枚举三级 severity，223×4×2×3=5352）；A.5.1 的哈希抽样描述属于未使用的 `bank` 块。
5. Table 34/46：**需修正文字**——Table 34 整表为 Bolt-only 未声明；Table 46 数值是三骨干宏平均而 episode 列是单骨干计数（760 应为 2280 或改措辞）；脚注 19.3%/44.5%/0.12 与图 6 的三骨干口径不一致。
6. Table 41：**需重算或改写**——SAITS/TATO 相同是因为都复用 backbone 调用宏平均（56.28/62.09/388.14→56/62/388），方法自身服务耗时从未测量；IntroAct 行为算术合成；cold start 2.6 s 无记录（实测加载 7.3/3.7/6.5 s）。
7. Table 6：**需修正文字**——缺失位差值被 `nan_to_num→0`（state.py:210-212），守约动作下四个幅度 intervention 特征恒为 0，仅 changed fraction 有效（=L-003）。
8. KEEP 骨干处理：**实现已确认、论文缺写**——三骨干均原生接收 NaN：Bolt/Chronos-2 官方掩码语义，TimesFM 官方插值+前缀剥离，适配层零填补。
9. Table 13：**已确认无误**——区间/p/Holm 同批 draws、parent 聚类、族=5 比较/骨干，全部对上。
10. SAITS 交叉拟合：**已确认无误**——真 out-of-fold、2 折、fit 于 bankx；仅"两半"措辞与实际交错分配略不符。
11. 数据用途：**已整理成表**——bankx=库/选参/训练，train_eval=验收，test=主评，test30/50=severity，test_m2/m3=掩码稳定性；bank/bankx2 未用于论文数字。
12. Table 40 Δ 方向：**已确认无误**——FULL−KEEP 负值改进，建议补符号说明并去掉 ↓。

另请注意：我无写入工具，`docs/p0_audit_20260922.md` 需由主 agent 保存上述报告全文。服务器侧只执行了只读操作（cat/ls/python 只读打印）。