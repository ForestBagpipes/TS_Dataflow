# IntroActTS 2026-09-20 v48 修订记录

## 交付

当前论文统一为 `latex/IntroActTS_20260920_v48.tex` 与同名 PDF。图1、图2使用作者更新的 BMP 无损生成 EPS，PPTX 与 BMP 原件保留。实验图3为干预频率与有害损失柱状图，图4为缺失程度折线图，图5为重建排名与预测效用排名热图。全部实验图从记录表格提取的 CSV 直接由 Python 生成，不经过 PPTX。

主结果和消融保留表格。主表去掉冗余类型列与 RMSSE 列，提高字号，RMSSE 在附录保留。长文字表格改用换行列，取消将小表强行放大到整页宽度。实验图统一无网格、字体、颜色与导出方式。

## 语言逻辑

阅读指定参考论文集合中十篇论文的摘要与首页，重点阅读 Data Shapley、LLMs Get Lost、Transformers Are Inherently Succinct 的引言及 Faster Cascades 的实验解读。学习的是问题具体化、方法与缺口对应、结论紧跟证据的结构，没有复制句子或新添参考文献。

摘要和引言围绕冻结模型下的逐请求修复决策展开。方法解释从历史可观测效用衔接到在线不可观测效用，再衔接局部估计和保留参考预测。结果按动作差异、主比较、有害干预、缺失程度、消融与成本展开。全文删除口语比喻、分号、引号和破折号式插语，削减重复转折与问答标题。

修正原有不受表格支持的解读，包括把消融差距称为最大、把平均排名解释为排除少数单元驱动、把单一混合均值差异归因于某个异常样本。保留移除干预特征和线性估计器的竞争性结果。主表逐骨干 MASE 对照现存 `results/v47/evaluation/test_*.json` 一致。

## 证据边界

v48 沿用最新填充稿中的已记录 v47 数字。本轮没有运行实验、训练或统计重算，也没有读取新封存标签。项目修订记录明确 v47 设计受旧 TEST 及 Chronos-2 结果启发，因此正文和附录披露事后评估，不再称 Chronos-2 独立未见确认。

保留的 SAITS 脚本明确 `FOLDS = 2`。原文四折和两折矛盾已修正，四折属于未在此验收的独立修复线。旧实现的其他隔离和 NaN 处理问题已由 `v47_verified_execution_20260919.md` 登记，不能以本轮排版成功代替其验收。因此本轮交付是完整文字与图表修订，不是研究结果重新认证或可直接投稿证明。

## 目录整理

自动审批拒绝了批量永久删除，理由是部分文件未逐项归档且可能丢失研究资产。随后使用更安全的完整移动归档，无永久删除。

当前目录只保留新版及依赖，旧稿、PDF、构建产物、脚本、旧图、审稿工具和记录全部保留在 `archive/paper_20260920/retired/`。源稿与数据溯源所需文件另存 `archive/paper_20260920/source/` 和 `figure/`。前者完整退休目录仅本地保留，历史已跟踪内容可由 Git 恢复。

## 验证与保存

`latex/scripts/build_paper.py` 完成 EPS 转换与 LaTeX/BibTeX 构建。`latex/scripts/audit_paper.py` 核验引用、五张图引用闭合、BMP 到 EPS 像素一致、CSV 对应原表和排版溢出。可视检查覆盖所有页面缩略图、两张示意图、正文实验图和附录表格。最终检查报告在 `latex/build/audit.json`。

当前 Windows 环境找不到 `codex-save-local`，已按约定在调用前检查 Git 状态。提交与远端状态另记真实执行结果，不把失败推送或命令缺失写成同步成功。
