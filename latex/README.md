# IntroAct-TS 论文

当前唯一入口为 `IntroActTS_20260923_v56.tex`，PDF 使用同名。正文九页，含参考文献、声明和附录共41页。

本轮依据作者的连贯自然写作规范直接修改原入口。修改前的完整源码、PDF和文献库备份在 `../archive/paper_20260923/pre_language/`。完整英文与中文逐段对照见 `IntroActTS_20260923_v56_中文逐段对照.md`，覆盖301个正文、标题及图表说明块，公式与49张数值表共用原始内容，表内文字附中文读法。

本版以2026-09-23工作区的最新实验内容为基础，原始输入快照在 `../archive/paper_20260923/pre_layout/`。没有回退到旧提交或重跑实验。

## 构建与检查

运行 `python latex/scripts/build_paper.py` 生成论文，运行 `python latex/scripts/audit_layout_v56.py` 检查布局、引用和表格数值保存情况。构建中间文件位于 `build/`。

正文图3为两行三列的通栏组图，绘图区等高等宽，六项方法图例也采用上三下三并铺满左右。冷色配色、竖柱、点形与线型统一，无背景网格。图注说明各面板、缩写、分母、聚合范围和点估计属性。图1和图2保持作者资产。

`build_figures_v56.py` 读取 `figure/data/v56_layout/` 中本轮冻结的三个CSV，生成 `fig8_diagnostics.eps`。manifest记录输入SHA-256。此前的绘图脚本和未提交数据保持可用，本版不覆盖它们的来源声明。

主表与消融表保留正文，边界与正文等宽，数字列右对齐。浅蓝底色突出完整方法，最优/次优标记遵守实际数值。49张表的数值与本轮输入快照一致。旧Holm表更新为读取v55保存统计量的27项比较，未重新计算统计检验。确认集最优、次优标记已按实际数值更正。部分相关工作、校准推导、案例及扩展诊断在附录，正文结论后紧接AI声明。

引言按背景、问题、现有局限、挑战和贡献组织，第二节为问题定义。当前结果与历史诊断分开说明，披露TEST复用、选择规则采用顺序、门控精度与伤害权衡，以及实测成本中SAITS实时计时与归档候选值的组合。验收依据为 `../docs/v55_acceptance_20260923.md` 和对应结果文件。该报告的交叉校准9/12与存储记录不一致，稿件采用记录中的8/12。

本版使用 `references_v56.bib` 保存本轮输入文献库快照。旧版 `audit_paper.py` 绑定旧版本及旧数据，不能用来验收v56。当前检查不代表新的文献核查、实验复现或投稿验收。

## 参考格式调整（2026-09-23）

本轮只修改文字、算法、表格浮动位置与附录结构，图文件和绘图脚本不变。备份位于 `../archive/paper_20260923/pre_reference_format/`。标题精简，挑战和贡献采用加粗括号编号，新增页首伪代码，附录按实验结果、方法设置和支持记录组织为12章，每章增加带数量的导读。正文9页，全文41页，完整中文对照301块，49张表数值保持一致。
