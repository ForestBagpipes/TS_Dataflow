# 框架图风险登记

## Readiness

`S0_FOUNDATION_READY_WITH_RISK`

## 已接受风险

1. **提示词与当前论文贡献中心不一致（minor）**  
   提示词把 CDP→SIR→CDS→SPO 作为四个并列创新阶段；当前论文明确把贡献中心放在独立验收层，并把提议者视作黑盒。下游图形采用融合版：保留四阶段阅读节奏，但降低策略学习的视觉权重。

2. **M3 不变量规约尚未展开（minor）**  
   图中不得把 M3 画成已实现模块。结构距离内部规则只以“局部/全局结构检验”表达。

3. **可预测性剥离 M4 是诊断性模块（minor）**  
   不把它画进主验收路径；如出现，只能作为行为风险旁的诊断注记。

4. **有限预算/UCB 细节不是当前方法文档的主贡献（minor）**  
   图2只保留“验收反馈→候选动作/预算”的次级外环，不把 UCB 作为核心算法盒。

5. **提议粒度适用性（caption burden）**  
   验收层适用于局部化提议器。该限制不塞进图内，保留给图注与正文。

## 下游强制规则

- 不得将行为风险命名为数据质量。
- 不得画成动作直接修改原始语料。
- 不得删除回滚路径或把回滚画成删除数据。
- 不得把三个验收量压成加权总分；必须是 AND 合取。
- 不得使用圆角矩形。
- 所有同级元素必须对齐并使用相同字号。
