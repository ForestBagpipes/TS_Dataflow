# -*- coding: utf-8 -*-
"""Introduction, written on the reference paper's sentence functions.

The previous attempt swung from copying its phrasing to abandoning its register
altogether, which lost the thing worth keeping. What that paper does well is a
fixed sequence of sentence functions carried in ordinary academic prose. The
opening states the shift in the abstract, the second sentence gives the reason,
the third defines the central notion, the fourth says why it matters, the fifth
brings in scale, the sixth turns scale into a problem and the seventh closes on
a need. That sequence is followed here. The wording differs where it can without
disturbing the sequence.
"""
import sys
sys.path.insert(0, "tools")
import docx
from docx_rewrite import count_math, find_paragraph, set_paragraph_md

PATH = "docx/胡宏彬-进度文档-20260820.docx"


def main():
    d = docx.Document(PATH)
    before = count_math(d)

    def rw(prefix, md):
        set_paragraph_md(d.paragraphs[find_paragraph(d, prefix)], md)

    # 第一段
    rw("风电场要预测明天的出力",
       "人工智能领域的研究重心正在从模型架构的设计转向数据本身。"
       "随着深度网络的结构设计逐渐成熟，数据质量已成为制约模型性能进一步提升的主要因素。"
       "在时序分析领域，这一趋势伴随时序基础模型的出现而愈发明确，"
       "此类模型在大规模多来源语料上完成预训练，"
       "再迁移至预测、分类与插补等下游任务[1-4]，"
       "以一套权重覆盖了以往需要逐个场景单独建模的工作，"
       "其能力上限因而主要取决于预训练语料的质量。"
       "时序语料的质量通常指语料在结构完备性与信噪水平两个维度上满足训练需求的程度。"
       "结构完备要求语料覆盖设备或市场实际出现过的各类运行模态，"
       "涵盖常规工况、状态切换以及罕见但真实发生的极端事件，"
       "模型只有在训练阶段接触过这些形态，部署时才具备相应的识别能力。"
       "信噪水平则要求传感器故障、通信丢包与人工录入错误所引入的失真足够少，"
       "此类失真一旦留存于语料之中，便会被模型当作规律学习[5,6]。"
       "随着工业物联网与金融交易系统的普及，可获取的时序记录正以极快的速度积累，"
       "而规模的扩张往往伴随噪声的累积，原始采集的数据在上述两个维度上均难以达标。"
       "鉴于此，如何从大规模含噪的时序记录中整备出高质量的预训练语料，"
       "已成为时序基础模型训练链条中一个亟待解决的问题。")

    # 第二段
    rw("围绕这件事已经积累了一批方法",
       "面向这一需求，时序数据治理作为高质量数据准备的重要环节，近年来受到持续关注[5,6]。"
       "治理技术所要回答的是语料中哪些部分存在问题以及这些问题应当如何处理，"
       "其具体做法或是依据既定约束改写可疑的观测值，"
       "或是估计各样本对模型的贡献并据此决定取舍。"
       "通过自动化的识别与处理，治理技术能够在控制人工投入的前提下提升语料的可用程度，"
       "使十万量级窗口的整备成为可能，同时保证不同批次之间处理标准的一致性。")

    # 第三段
    rw("放到时序数据上，治理有一处别处没有的麻烦",
       "然而，时序数据的治理面临一个其他数据类型所不具备的根本困难，"
       "即一次修改的有害与否往往取决于被修改的数据段本身，而与修改方式无关。"
       "以电力变压器的监测记录为例，设备检修期间产生的整段水平漂移应当被处理，"
       "若予以保留，模型会将偏移后的水平当作正常基线，"
       "在随后的正常时段持续给出偏高的预测，误差可延续至漂移结束之后很久。"
       "而一次真实发生的负荷突增在波形上与故障尖峰高度相似，"
       "它却是语料结构完备性所依赖的关键部分，"
       "将其削平相当于删去模型最需要学习的稀有模态。"
       "两种情形所面对的是同一个去尖峰算子与同一组参数，"
       "一次构成修复，另一次构成破坏，差异仅在于该数据段本身承载的内容。"
       "这意味着依据事先设定的规则去判断某次修改是否值得执行，在原理层面便无法成立。")

    # 第四段 梳理总起
    rw("已有做法可以按两件事归类",
       "为系统评估现有方法在这一问题上的适用性，"
       "本文对时序数据治理与选择技术进行了梳理与分类，见表 1。"
       "依据判定发生的时刻与判定所依据的信息，当前工作大致可分为三类。")

    # 三条路线
    rw("（1）修复式清洗看取值是否越过了事先给定的界",
       "（1）修复式清洗以取值是否越出事先给定的边界作为判定依据。"
       "SCREEN[5]假定相邻时间步之间的变化幅度存在上下限，"
       "将越界的观测点修复至最近的合法取值，并给出了线性时间的在线算法。"
       "IMR[6]转而采用自回归模型逐点估计并迭代收敛。"
       "MTCSC[8]将速度约束与聚类相结合，借助维度之间的相关性提升修复精度。"
       "这一类方法的判定在算子选定之时即告完成，规则给出结论后，改写随即作用于原始数值。")
    rw("（2）打分式选择看这条数据对模型有多大用处",
       "（2）打分式选择以样本对模型的估计贡献作为判定依据。"
       "Data Shapley[9]借助合作博弈中的 Shapley 值刻画样本价值，"
       "Data-OOB[10]改用袋装模型的袋外估计以降低计算开销，"
       "TimeInf[11]将影响函数定义于时间块之上以保留时序结构，"
       "TSRating[12]则通过大模型的成对比较蒸馏出轻量评分器。"
       "这一类方法产出的是分数与排序，语料依分数取舍而自身不被改动。")
    rw("（3）学习式编排看哪个算子在下游能换来更高的回报",
       "（3）学习式编排以下游反馈的期望回报作为判定依据。"
       "清洗智能体[13]采用分层策略决定在何种窗口上调用何种算子，"
       "并将下游误差的变化作为奖励，策略可随训练逐步改进。"
       "这一类方法将判定交由学习所得的价值估计，而算子在训练过程中即已生效。")

    # 局限
    rw("这三类在各自的设定里都能跑出不错的结果",
       "上述方法在各自设定的场景下均能取得良好效果，"
       "但面对本文所关注的问题时，普遍存在以下两方面局限。")
    rw("1）判断落在动作之前，做错了退不回来",
       "1）判定时刻早于执行时刻，错误决策无从撤销。"
       "修复式清洗在规则给出结论的同时便将改写作用于原始数值，"
       "此后不再具备核对该次改写是否达到预期的机制，"
       "SCREEN[5]与 MTCSC[8]均是如此，速度约束一旦判定某点越界，替换随即发生。"
       "学习式编排虽允许策略从反馈中改进，"
       "但清洗智能体[13]的算子同样在训练过程中立即生效，"
       "每一次探索失误都实际作用于语料，策略训练越充分则语料损伤越严重。"
       "打分式选择规避了这一问题，其代价在于完全不产生动作，"
       "面对局部含有尖峰的窗口，Data Shapley[9]与 TimeInf[11]这类方法只能整窗丢弃，"
       "尖峰之外完好的部分一并舍去。"
       "三条路线因此陷入同一困境，具备动作能力者缺乏撤销机制，具备撤销能力者不产生动作。")
    rw("2）判断只靠一路信息",
       "2）判定所依据的信息来源单一，"
       "且该信息在最需要审慎处理的数据上会给出方向相反的读数。"
       "修复式清洗仅考察取值是否满足约束，"
       "而一次真实的极端事件在数值上同样越界，约束本身无法区分这两种情形。"
       "打分式选择与学习式编排最终均以模型表现作为依据，"
       "问题在于提升模型预测精度存在两条途径，修复缺陷是其一，"
       "将序列削平使其更易预测是其二，后者同样能够降低误差。"
       "在稀有事件所处的窗口上，这两条途径给出的读数恰好相反，"
       "仅凭模型意见作出判定，所放行的往往正是最应被拦截的修改。")

    # 挑战
    rw("要补上这两块短板",
       "针对上述局限，当前需要一种能够在动作执行之后进行判定、"
       "并允许撤销错误决策的治理方法，而这类方法的构筑面临多重挑战。"
       "其一在于判定依据的来源本身带有偏差。"
       "修改的有害与否只能从效果中读出，而能够报告效果的仪器唯有目标模型，"
       "上述第二条局限说明这一仪器在最关键的场景下并不可信，"
       "因此判定不能仅凭模型意见，尚需一路不依赖模型的证据与之相互印证，"
       "而该证据的阈值又无法凭领域经验事先设定。"
       "其二在于试错的代价作用于不可恢复的对象。"
       "策略从判定结果中学习需要反复尝试，"
       "而语料与游戏或仿真环境不同，其不存在可以重置的副本，被削平的峰值无法找回。"
       "如何使学习过程既积累足够经验，又不在积累过程中损伤语料，"
       "是将判定推迟至执行之后所必须一并解决的问题。")

    # 方法
    rw("IntroAct-TS 按这个路子搭建",
       "为解决判定依据存在偏差与试错代价不可恢复这两个问题，"
       "本文提出屏蔽强化学习驱动的时序数据治理智能体 IntroAct-TS，"
       "将治理建模为带预算的受约束序贯决策。"
       "IntroAct-TS 首先从窗口自身的统计画像与冻结基础模型的行为反应中获取证据，"
       "并以画像相近的邻居为参照实施稳健标准化，使不同窗口之间的读数具备可比性。"
       "随后将候选动作施加于工作副本的拷贝之上试执行并重新测量，"
       "原始数值在此期间保持不变，"
       "由此每次尝试均有确切后果可读，而尝试本身不留下痕迹。"
       "针对判定依据存在偏差这一挑战，"
       "方法要求模型效用改善与时序结构受控两个相互独立的条件同时成立方予提交，"
       "其中结构条件完全不参考模型意见，其阈值交由共形风险控制标定，"
       "从而将阈值的设定由经验判断转化为统计问题。"
       "针对试错代价这一挑战，由于全部候选均在副本上执行且提交需经判定，"
       "未通过的尝试不改动任何数据，学习过程因而无需以损伤语料为代价。"
       "每次判定的结果最终回流为策略的奖励，"
       "被撤销的动作仅留下探测开销而不获正回报，"
       "策略据此更新动作价值估计并在窗口之间分配探测预算，"
       "从而提升了治理在结构复杂且含噪的时序语料上的可靠性与效率。"
       "图 1 以一对波形相近的窗口说明这一过程如何给出相反的裁决。")

    d.save(PATH)
    d2 = docx.Document(PATH)
    print(f"引言重写完成，公式 {before} -> {count_math(d2)}")


if __name__ == "__main__":
    main()
