# -*- coding: utf-8 -*-
"""Chapter 3, theorems 2 to 5, section 3.4 and 3.5. Adds the incremental update
and budget allocation formulas, which the previous draft described in words."""
import sys
sys.path.insert(0, "tools")
import docx
from docx_rewrite import (count_math, find_paragraph, insert_after,
                          set_paragraph_md)

PATH = "docx/胡宏彬-进度文档-20260819.docx"


def main():
    d = docx.Document(PATH)
    before = count_math(d)

    def rw(prefix, md):
        set_paragraph_md(d.paragraphs[find_paragraph(d, prefix)], md)

    rw("定理 2（非阻塞性）",
       "定理 2（非阻塞性）在任意状态与任意阈值设定下，屏蔽后的可行动作集非空。")
    rw("证明。考察保留动作",
       r"证明。考察保留动作。它不修改数据，因此 $D(x,x)=0$ 对任意 $\tau>0$ 都满足结构条件，"
       "其探测成本为零，对任意剩余预算都满足预算约束。"
       "于是保留动作在任意状态下都可行，屏蔽后的动作集至少包含它。证毕。")
    rw("定理 2 对强化学习的意义在于",
       "定理 2 对强化学习的意义在于，屏蔽不会让策略陷入无动作可选的状态，"
       "因此可以把屏蔽视为对动作空间的状态相关限制，而非会中断决策过程的外部干预。"
       "这也是 3.4 节遗憾分析成立的前提。")

    rw("定理 3（探索的结构安全性）",
       r"定理 3（探索的结构安全性）对任意策略 $\pi$ 与任意探索强度，"
       r"被提交的每一次编辑都满足 $D(x,a(x))<\tau$，"
       "因此治理后语料的结构失真上界与策略无关。")
    rw("证明。由算法 2，动作作用于副本的拷贝",
       r"证明。由算法 2，动作作用于副本的拷贝，工作副本只在第 5 行被更新，"
       r"而进入第 5 行的前提是三个条件同时成立，其中包含 $d<\tau$。"
       r"未通过判据的动作在第 7 行只写入轨迹与奖励，不改变工作副本。"
       r"策略 $\pi$ 的作用是决定提出哪些候选，它不影响判据本身。"
       r"因此无论 $\pi$ 处于何种训练阶段、以多大概率进行随机探索，"
       "凡被提交的编辑都满足结构约束。证毕。")
    rw("定理 3 是本文与算子立即生效的强化学习清洗方案的核心差异",
       "定理 3 是本文与算子立即生效的强化学习清洗方案的核心差异。"
       "在那类方案中，训练期间的每一次错误探索都在真实语料上留下痕迹，"
       "安全性只能在收敛后的期望意义上成立。本文的探索发生在副本上，"
       "未通过屏蔽的尝试不留下任何后果，学习过程因而不构成破坏过程。"
       "这一性质使智能体可以在真实语料上直接学习，不需要先在仿真环境中预训练。")

    rw("结构条件给出的是确定性的上界",
       "结构条件给出的是确定性的上界，损害率的概率性保证则由阈值标定提供。")
    rw("定理 4（风险控制）",
       "定理 4（风险控制）设校准集与部署数据可交换，风险函数有界且关于阈值单调不减，"
       r"则由共形风险控制在目标水平 $\alpha$ 下选出的阈值 $\tau$ 满足，"
       r"部署数据上被提交编辑的损害率的期望不超过 $\alpha$。")
    rw("证明。该结论是共形风险控制主定理[21]",
       r"证明。该结论是共形风险控制主定理[21]在本文风险函数下的实例。"
       r"记校准集大小为 $n$，风险函数取定义 1 中的零一指示，取值于 $[0,1]$，有界性自动满足。"
       "可交换性保证部署样本的风险与校准样本的风险同分布可交换，"
       r"按 2.1 节的选择规则取 $\tau$ 为使经验风险加上有限样本修正项不超过 $\alpha$ 的最大阈值，"
       r"则部署风险的期望受 $\alpha$ 控制。证毕。")
    rw("两点边界需要说明",
       "有两点边界。其一是可达风险下界，阈值网格中最保守的取值仍然对应一个正的经验风险，"
       "低于这个下界的目标水平在当前动作空间下无解，此时标定过程返回最保守的阈值，"
       "这一行为符合定义。其二是可交换性在策略学习过程中的脆弱性，"
       "策略更新会改变候选动作的分布，从而改变提交编辑的分布，"
       "使部署数据与早先的校准集不再可交换。"
       r"实现中的处理是每 $T_{\mathrm{cal}}$ 次决策重新标定一次，把两次标定之间的阈值视为固定值。"
       "定理 3 给出的结构上界在此期间仍然成立，"
       "因此重新标定的间隔只影响损害率保证的紧致程度，不影响结构安全性。")

    # 3.4
    rw("前三个模块构成了一个可以安全试错的环境",
       "前三个模块构成了一个可以安全试错的环境，SPO 在这个环境中学习动作选择。"
       "设计这一模块的动机来自一个实测观察。在固定规则的版本中，"
       "被否决后更换动作重试的净效果为负，更换后的动作改善与恶化之比不足首次提议的十分之一。"
       "原因在于固定规则把最有把握的动作排在首位，此后的备选顺序与窗口特性无关，"
       "更换因而往往是盲目的。这说明序贯决策本身可行，问题出在备选顺序需要根据窗口类型学习。")
    rw("SPO 采用上下文老虎机而非深度强化学习",
       "SPO 采用上下文老虎机而非深度强化学习，理由是本任务的决策序列很短，"
       "多数窗口在三步以内结束，状态经 CDP 提取后维度不高，动作集合规模有限。"
       "在这样的设定下，上置信界方法的样本效率与可解释性都优于需要大量交互的深度方法，"
       "且有成熟的遗憾分析[18,19,20]。")
    rw("具体做法是按画像簇维护动作价值",
       r"具体做法是按画像簇维护动作价值。对第 $j$ 个画像簇与动作 $a$，"
       r"维护估计值 $Q(j,a)$ 与访问计数 $n(j,a)$，动作选择依据上置信界")
    rw("a_t = arg max_{a ∈ A_shield(s)} [ Q(j, a) + c_u · sqrt( 2 ln t / n(j, a) ) ]",
       r"$$a_{t}=\arg\max_{a\in\mathcal{A}_{\mathrm{shield}}(s)}"
       r"\left[Q(j,a)+c_{u}\sqrt{\frac{2\ln t}{n(j,a)}}\right]$$")

    idx = find_paragraph(d, "其中 A_shield(s) 是屏蔽后的可行动作集")
    set_paragraph_md(d.paragraphs[idx],
       r"其中 $\mathcal{A}_{\mathrm{shield}}(s)$ 是屏蔽后的可行动作集，由定理 2 保证非空，"
       r"$t$ 为全局决策计数，$c_{u}$ 控制探索强度。收到 CDS 的奖励 $r$ 后按增量式更新")
    insert_after(d, idx, [
        r"$$n(j,a)\leftarrow n(j,a)+1,\qquad "
        r"Q(j,a)\leftarrow Q(j,a)+\frac{r-Q(j,a)}{n(j,a)}$$",
        "同一窗口内发生撤销时，撤销原因并入上下文，"
        "使被结构条件否决与被效用条件否决对应不同的备选分布，"
        "这正是固定规则版本所缺少的部分。",
    ], template_idx=idx)

    rw("价值表按撤销原因分为三张而非一张",
       r"价值表按撤销原因分为三张。$Q_{0}$ 用于窗口内的首次提议，"
       r"$Q_{\mathrm{struct}}$ 用于被结构条件否决后的重试，"
       r"$Q_{\mathrm{util}}$ 用于被效用条件否决后的重试。"
       "分开维护而不在单张表上加一维特征，理由是两类否决指向不同的备选方向。"
       "结构否决意味着足迹过大，后续应换足迹更小的算子。"
       "效用否决意味着缺陷判断本身可能有误，后续应重新考虑窗口中存在的是哪一类缺陷。"
       "单张表会把这两种方向平均成一个次序，而这正是固定规则版本表现出的失效方式。"
       r"每张表按簇数与算子数建全表，$k$ 取 12 时为 12 乘 4。")

    rw("暖启动用固定规则策略的历史轨迹填充",
       "暖启动用固定规则策略的历史轨迹填充。每个格子的初值取该格子历史上实际获得的屏蔽奖励的均值，"
       "访问计数取历史样本数与上限的较小者。这样取值使暖启动同时编码了负面证据，"
       "被撤销的候选按探测代价计入负奖励，因此一个历来无一通过的格子初值低于零，"
       "而未被访问过的格子取乐观初值高于零，策略据此自然地优先尝试未访问格而回避已知无效格，"
       "不需要为回避已知无效格另设机制。若改用接受率乘平均效用增益，"
       "无一通过的格子会得到零，从未有效与从未尝试就分不开了。"
       "重分段一列不按簇分别填充，而是整列取其全局均值，"
       "依据是该算子的接受率在簇之间没有可检出的差异，按簇填充等于把抽样噪声写成先验。")
    rw("样本量过小的画像簇合并处理",
       "样本量过小的画像簇需要合并。由个位数窗口估计出的价值不构成估计，"
       "因此规模低于下限的簇并入按质心余弦相似度最近的达标簇，规模下限取 5 个窗口。")

    rw("接受奖励需要设上界",
       "接受奖励需要设上界。定理 5 的遗憾界要求奖励有界或次高斯，"
       "而实测的效用变化两者皆非，其分布呈长尾，少数窗口的读数比中位数高出两个数量级，"
       "远超上置信界在同等规模下的探索宽度。若不设上界，"
       "持有极端值的格子将被永久选中而探索停止，这破坏的是定理的前提。"
       r"因此接受奖励按训练划分上的分位数 $q$ 做单侧截断，"
       r"即 $r=\min(\Delta U,q)$，评测划分不参与该数值的确定。"
       "撤销奖励本身已由代价系数限定，不需要截断。"
       "分位数取值经过敏感性检验，取值不宜低于 95 分位，"
       "更低的取值会压到奖励分布的主体而非尾部，改变相当一部分窗口的动作选择。")

    idx = find_paragraph(d, "预算分配建立在同一套价值估计之上")
    set_paragraph_md(d.paragraphs[idx],
       r"预算分配建立在同一套价值估计之上。给定语料总预算 $B$，"
       r"SPO 用 $\max_{a}Q(j,a)$ 估计在窗口 $x_{i}$ 上继续探测的期望收益，"
       r"再按收益与预估代价 $\hat{c}_{i}$ 之比对窗口排序")
    insert_after(d, idx, [
        r"$$\rho_{i}=\frac{\max_{a}Q(j_{i},a)}{\hat{c}_{i}},\qquad "
        r"b_{i}=\begin{cases}0,&\text{窗口后验确定且指向干净}\\"
        r"\min(\hat{c}_{i},B_{\mathrm{left}}),&\text{按 }\rho_{i}\text{ 降序}\end{cases}$$",
        "缺陷类型后验接近确定且指向干净的窗口获得零预算并被直接跳过，"
        "后验分散的窗口则获得更多次尝试机会。"
        "这一机制把探测集中在证据模糊的地方，其效果在 4.2 节以预算效率曲线衡量。"
        "算法 3 给出完整流程。",
    ], template_idx=idx)

    rw("屏蔽把动作空间限制为状态相关的子集",
       "屏蔽把动作空间限制为状态相关的子集，这一限制不会破坏学习的收敛性质。")
    rw("定理 5（屏蔽动作空间上的遗憾界）",
       "定理 5（屏蔽动作空间上的遗憾界）设奖励关于状态特征线性可表示且取值有界，"
       r"屏蔽后的动作集 $\mathcal{A}_{\mathrm{shield}}(s)$ 在每个状态下非空，"
       "则算法 3 采用的上置信界策略相对于最优屏蔽策略的累积遗憾为 "
       r"$O(\sqrt{Td\log T})$，其中 $T$ 为总决策轮数，$d$ 为状态特征维度。")
    rw("证明。由定理 2，A_shield(s) 在任意状态下非空",
       r"证明。由定理 2，$\mathcal{A}_{\mathrm{shield}}(s)$ 在任意状态下非空，"
       "因此屏蔽构成一个状态相关的动作集限制，而不是会使决策中断的约束。"
       "带受限动作集的上下文老虎机的上置信界分析在此直接适用[19]，"
       "其遗憾界形式与动作集大小无关，只依赖特征维度与轮数。"
       "将比较基准取为屏蔽后可行动作中的最优策略，即得结论。证毕。")
    rw("有界性这一假设在实现中由奖励截断保证",
       "有界性这一假设在实现中由奖励截断保证。"
       "实测的效用变化呈长尾分布，不加处理则该假设不成立，遗憾界的推导前提随之失效。"
       "实现对接受奖励按训练划分的分位数做单侧截断，使实现与定理严格对应。")
    rw("定理 5 说明屏蔽在提供安全保证的同时",
       "定理 5 说明屏蔽在提供安全保证的同时保留了策略学习的渐近性质。"
       "比较基准是屏蔽后的最优策略而非无约束的最优策略，"
       r"两者之间的差距对应屏蔽为安全付出的代价，其大小由 $\tau$ 决定，"
       r"而 $\tau$ 又由目标损害率 $\alpha$ 确定。"
       "这一设计把安全与效率的权衡集中在一个具有明确统计含义的参数上。")

    # 3.5
    rw("设语料窗口数为 N，窗口长度为 L",
       r"设语料窗口数为 $N$，窗口长度为 $L$，模型池大小为 $M$，"
       r"每个窗口的平均动作尝试次数为 $T$，邻域大小为 $K$，画像簇数为 $J$，"
       r"单次模型前向的开销为 $C(L)$。"
       r"CDP 阶段中，画像提取的主要开销来自频谱分析与变点检测，为 $O(NL\log L)$，"
       r"行为探测需要对每个窗口在每个模型上做一次前向，为 $O(NMC(L))$，"
       r"同类邻居检索采用近似索引后为 $O(N\log N+NK)$。"
       r"SIR 阶段中，每次候选动作包含一次算子执行与一次重测，为 $O(L+C(L))$，"
       r"该阶段总开销为 $O(NT(L+C(L)))$。"
       r"CDS 中结构失真的计算是纯统计操作，为 $O(L\log L)$，不涉及模型前向。"
       r"SPO 的价值更新是常数时间操作，预算分配需要对窗口排序，为 $O(N\log N)$，"
       r"价值表的空间开销为 $O(J|\mathcal{A}|)$。"
       r"阈值标定在已缓存的损害记录上进行，为 $O(|\Lambda|N)$，其中 $|\Lambda|$ 为阈值网格大小。")
    rw("整体开销的主导项是模型前向次数",
       r"整体开销的主导项是模型前向次数 $O(N(M+T)C(L))$。"
       "探测只做前向而不需要反向传播，其单位开销显著低于基于影响函数的估值方法[11]。"
       "引入策略学习没有改变复杂度的主导项，价值更新与预算分配都是轻量操作，"
       r"而预算分配会减少 $T$ 的平均值，实际开销通常低于均匀分配预算的版本。"
       "阈值标定与结构度量都可以在缓存结果上离线完成，"
       "调整目标风险水平不需要重新占用加速器。")

    d.save(PATH)
    d2 = docx.Document(PATH)
    print(f"第 3 章完成，公式 {before} -> {count_math(d2)}")


if __name__ == "__main__":
    main()
